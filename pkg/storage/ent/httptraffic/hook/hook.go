// Code generated by ent, DO NOT EDIT.

package hook

import (
	"context"
	"fmt"

	"danny.vn/hotpot/pkg/storage/ent/httptraffic"
)

// The SilverHttptrafficClientIp5mFunc type is an adapter to allow the use of ordinary
// function as SilverHttptrafficClientIp5m mutator.
type SilverHttptrafficClientIp5mFunc func(context.Context, *httptraffic.SilverHttptrafficClientIp5mMutation) (httptraffic.Value, error)

// Mutate calls f(ctx, m).
func (f SilverHttptrafficClientIp5mFunc) Mutate(ctx context.Context, m httptraffic.Mutation) (httptraffic.Value, error) {
	if mv, ok := m.(*httptraffic.SilverHttptrafficClientIp5mMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *httptraffic.SilverHttptrafficClientIp5mMutation", m)
}

// The SilverHttptrafficTraffic5mFunc type is an adapter to allow the use of ordinary
// function as SilverHttptrafficTraffic5m mutator.
type SilverHttptrafficTraffic5mFunc func(context.Context, *httptraffic.SilverHttptrafficTraffic5mMutation) (httptraffic.Value, error)

// Mutate calls f(ctx, m).
func (f SilverHttptrafficTraffic5mFunc) Mutate(ctx context.Context, m httptraffic.Mutation) (httptraffic.Value, error) {
	if mv, ok := m.(*httptraffic.SilverHttptrafficTraffic5mMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *httptraffic.SilverHttptrafficTraffic5mMutation", m)
}

// The SilverHttptrafficUserAgent5mFunc type is an adapter to allow the use of ordinary
// function as SilverHttptrafficUserAgent5m mutator.
type SilverHttptrafficUserAgent5mFunc func(context.Context, *httptraffic.SilverHttptrafficUserAgent5mMutation) (httptraffic.Value, error)

// Mutate calls f(ctx, m).
func (f SilverHttptrafficUserAgent5mFunc) Mutate(ctx context.Context, m httptraffic.Mutation) (httptraffic.Value, error) {
	if mv, ok := m.(*httptraffic.SilverHttptrafficUserAgent5mMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *httptraffic.SilverHttptrafficUserAgent5mMutation", m)
}

// Condition is a hook condition function.
type Condition func(context.Context, httptraffic.Mutation) bool

// And groups conditions with the AND operator.
func And(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m httptraffic.Mutation) bool {
		if !first(ctx, m) || !second(ctx, m) {
			return false
		}
		for _, cond := range rest {
			if !cond(ctx, m) {
				return false
			}
		}
		return true
	}
}

// Or groups conditions with the OR operator.
func Or(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m httptraffic.Mutation) bool {
		if first(ctx, m) || second(ctx, m) {
			return true
		}
		for _, cond := range rest {
			if cond(ctx, m) {
				return true
			}
		}
		return false
	}
}

// Not negates a given condition.
func Not(cond Condition) Condition {
	return func(ctx context.Context, m httptraffic.Mutation) bool {
		return !cond(ctx, m)
	}
}

// HasOp is a condition testing mutation operation.
func HasOp(op httptraffic.Op) Condition {
	return func(_ context.Context, m httptraffic.Mutation) bool {
		return m.Op().Is(op)
	}
}

// HasAddedFields is a condition validating `.AddedField` on fields.
func HasAddedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m httptraffic.Mutation) bool {
		if _, exists := m.AddedField(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.AddedField(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasClearedFields is a condition validating `.FieldCleared` on fields.
func HasClearedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m httptraffic.Mutation) bool {
		if exists := m.FieldCleared(field); !exists {
			return false
		}
		for _, field := range fields {
			if exists := m.FieldCleared(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasFields is a condition validating `.Field` on fields.
func HasFields(field string, fields ...string) Condition {
	return func(_ context.Context, m httptraffic.Mutation) bool {
		if _, exists := m.Field(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.Field(field); !exists {
				return false
			}
		}
		return true
	}
}

// If executes the given hook under condition.
//
//	hook.If(ComputeAverage, And(HasFields(...), HasAddedFields(...)))
func If(hk httptraffic.Hook, cond Condition) httptraffic.Hook {
	return func(next httptraffic.Mutator) httptraffic.Mutator {
		return httptraffic.MutateFunc(func(ctx context.Context, m httptraffic.Mutation) (httptraffic.Value, error) {
			if cond(ctx, m) {
				return hk(next).Mutate(ctx, m)
			}
			return next.Mutate(ctx, m)
		})
	}
}

// On executes the given hook only for the given operation.
//
//	hook.On(Log, httptraffic.Delete|httptraffic.Create)
func On(hk httptraffic.Hook, op httptraffic.Op) httptraffic.Hook {
	return If(hk, HasOp(op))
}

// Unless skips the given hook only for the given operation.
//
//	hook.Unless(Log, httptraffic.Update|httptraffic.UpdateOne)
func Unless(hk httptraffic.Hook, op httptraffic.Op) httptraffic.Hook {
	return If(hk, Not(HasOp(op)))
}

// FixedError is a hook returning a fixed error.
func FixedError(err error) httptraffic.Hook {
	return func(httptraffic.Mutator) httptraffic.Mutator {
		return httptraffic.MutateFunc(func(context.Context, httptraffic.Mutation) (httptraffic.Value, error) {
			return nil, err
		})
	}
}

// Reject returns a hook that rejects all operations that match op.
//
//	func (T) Hooks() []httptraffic.Hook {
//		return []httptraffic.Hook{
//			Reject(httptraffic.Delete|httptraffic.Update),
//		}
//	}
func Reject(op httptraffic.Op) httptraffic.Hook {
	hk := FixedError(fmt.Errorf("%s operation is not allowed", op))
	return On(hk, op)
}

// Chain acts as a list of hooks and is effectively immutable.
// Once created, it will always hold the same set of hooks in the same order.
type Chain struct {
	hooks []httptraffic.Hook
}

// NewChain creates a new chain of hooks.
func NewChain(hooks ...httptraffic.Hook) Chain {
	return Chain{append([]httptraffic.Hook(nil), hooks...)}
}

// Hook chains the list of hooks and returns the final hook.
func (c Chain) Hook() httptraffic.Hook {
	return func(mutator httptraffic.Mutator) httptraffic.Mutator {
		for i := len(c.hooks) - 1; i >= 0; i-- {
			mutator = c.hooks[i](mutator)
		}
		return mutator
	}
}

// Append extends a chain, adding the specified hook
// as the last ones in the mutation flow.
func (c Chain) Append(hooks ...httptraffic.Hook) Chain {
	newHooks := make([]httptraffic.Hook, 0, len(c.hooks)+len(hooks))
	newHooks = append(newHooks, c.hooks...)
	newHooks = append(newHooks, hooks...)
	return Chain{newHooks}
}

// Extend extends a chain, adding the specified chain
// as the last ones in the mutation flow.
func (c Chain) Extend(chain Chain) Chain {
	return c.Append(chain.hooks...)
}
