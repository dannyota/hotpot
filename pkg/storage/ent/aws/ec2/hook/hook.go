// Code generated by ent, DO NOT EDIT.

package hook

import (
	"context"
	"fmt"

	"danny.vn/hotpot/pkg/storage/ent/aws/ec2"
)

// The BronzeAWSEC2InstanceFunc type is an adapter to allow the use of ordinary
// function as BronzeAWSEC2Instance mutator.
type BronzeAWSEC2InstanceFunc func(context.Context, *ec2.BronzeAWSEC2InstanceMutation) (ec2.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeAWSEC2InstanceFunc) Mutate(ctx context.Context, m ec2.Mutation) (ec2.Value, error) {
	if mv, ok := m.(*ec2.BronzeAWSEC2InstanceMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *ec2.BronzeAWSEC2InstanceMutation", m)
}

// The BronzeAWSEC2InstanceTagFunc type is an adapter to allow the use of ordinary
// function as BronzeAWSEC2InstanceTag mutator.
type BronzeAWSEC2InstanceTagFunc func(context.Context, *ec2.BronzeAWSEC2InstanceTagMutation) (ec2.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeAWSEC2InstanceTagFunc) Mutate(ctx context.Context, m ec2.Mutation) (ec2.Value, error) {
	if mv, ok := m.(*ec2.BronzeAWSEC2InstanceTagMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *ec2.BronzeAWSEC2InstanceTagMutation", m)
}

// The BronzeHistoryAWSEC2InstanceFunc type is an adapter to allow the use of ordinary
// function as BronzeHistoryAWSEC2Instance mutator.
type BronzeHistoryAWSEC2InstanceFunc func(context.Context, *ec2.BronzeHistoryAWSEC2InstanceMutation) (ec2.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeHistoryAWSEC2InstanceFunc) Mutate(ctx context.Context, m ec2.Mutation) (ec2.Value, error) {
	if mv, ok := m.(*ec2.BronzeHistoryAWSEC2InstanceMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *ec2.BronzeHistoryAWSEC2InstanceMutation", m)
}

// The BronzeHistoryAWSEC2InstanceTagFunc type is an adapter to allow the use of ordinary
// function as BronzeHistoryAWSEC2InstanceTag mutator.
type BronzeHistoryAWSEC2InstanceTagFunc func(context.Context, *ec2.BronzeHistoryAWSEC2InstanceTagMutation) (ec2.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeHistoryAWSEC2InstanceTagFunc) Mutate(ctx context.Context, m ec2.Mutation) (ec2.Value, error) {
	if mv, ok := m.(*ec2.BronzeHistoryAWSEC2InstanceTagMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *ec2.BronzeHistoryAWSEC2InstanceTagMutation", m)
}

// Condition is a hook condition function.
type Condition func(context.Context, ec2.Mutation) bool

// And groups conditions with the AND operator.
func And(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m ec2.Mutation) bool {
		if !first(ctx, m) || !second(ctx, m) {
			return false
		}
		for _, cond := range rest {
			if !cond(ctx, m) {
				return false
			}
		}
		return true
	}
}

// Or groups conditions with the OR operator.
func Or(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m ec2.Mutation) bool {
		if first(ctx, m) || second(ctx, m) {
			return true
		}
		for _, cond := range rest {
			if cond(ctx, m) {
				return true
			}
		}
		return false
	}
}

// Not negates a given condition.
func Not(cond Condition) Condition {
	return func(ctx context.Context, m ec2.Mutation) bool {
		return !cond(ctx, m)
	}
}

// HasOp is a condition testing mutation operation.
func HasOp(op ec2.Op) Condition {
	return func(_ context.Context, m ec2.Mutation) bool {
		return m.Op().Is(op)
	}
}

// HasAddedFields is a condition validating `.AddedField` on fields.
func HasAddedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m ec2.Mutation) bool {
		if _, exists := m.AddedField(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.AddedField(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasClearedFields is a condition validating `.FieldCleared` on fields.
func HasClearedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m ec2.Mutation) bool {
		if exists := m.FieldCleared(field); !exists {
			return false
		}
		for _, field := range fields {
			if exists := m.FieldCleared(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasFields is a condition validating `.Field` on fields.
func HasFields(field string, fields ...string) Condition {
	return func(_ context.Context, m ec2.Mutation) bool {
		if _, exists := m.Field(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.Field(field); !exists {
				return false
			}
		}
		return true
	}
}

// If executes the given hook under condition.
//
//	hook.If(ComputeAverage, And(HasFields(...), HasAddedFields(...)))
func If(hk ec2.Hook, cond Condition) ec2.Hook {
	return func(next ec2.Mutator) ec2.Mutator {
		return ec2.MutateFunc(func(ctx context.Context, m ec2.Mutation) (ec2.Value, error) {
			if cond(ctx, m) {
				return hk(next).Mutate(ctx, m)
			}
			return next.Mutate(ctx, m)
		})
	}
}

// On executes the given hook only for the given operation.
//
//	hook.On(Log, ec2.Delete|ec2.Create)
func On(hk ec2.Hook, op ec2.Op) ec2.Hook {
	return If(hk, HasOp(op))
}

// Unless skips the given hook only for the given operation.
//
//	hook.Unless(Log, ec2.Update|ec2.UpdateOne)
func Unless(hk ec2.Hook, op ec2.Op) ec2.Hook {
	return If(hk, Not(HasOp(op)))
}

// FixedError is a hook returning a fixed error.
func FixedError(err error) ec2.Hook {
	return func(ec2.Mutator) ec2.Mutator {
		return ec2.MutateFunc(func(context.Context, ec2.Mutation) (ec2.Value, error) {
			return nil, err
		})
	}
}

// Reject returns a hook that rejects all operations that match op.
//
//	func (T) Hooks() []ec2.Hook {
//		return []ec2.Hook{
//			Reject(ec2.Delete|ec2.Update),
//		}
//	}
func Reject(op ec2.Op) ec2.Hook {
	hk := FixedError(fmt.Errorf("%s operation is not allowed", op))
	return On(hk, op)
}

// Chain acts as a list of hooks and is effectively immutable.
// Once created, it will always hold the same set of hooks in the same order.
type Chain struct {
	hooks []ec2.Hook
}

// NewChain creates a new chain of hooks.
func NewChain(hooks ...ec2.Hook) Chain {
	return Chain{append([]ec2.Hook(nil), hooks...)}
}

// Hook chains the list of hooks and returns the final hook.
func (c Chain) Hook() ec2.Hook {
	return func(mutator ec2.Mutator) ec2.Mutator {
		for i := len(c.hooks) - 1; i >= 0; i-- {
			mutator = c.hooks[i](mutator)
		}
		return mutator
	}
}

// Append extends a chain, adding the specified hook
// as the last ones in the mutation flow.
func (c Chain) Append(hooks ...ec2.Hook) Chain {
	newHooks := make([]ec2.Hook, 0, len(c.hooks)+len(hooks))
	newHooks = append(newHooks, c.hooks...)
	newHooks = append(newHooks, hooks...)
	return Chain{newHooks}
}

// Extend extends a chain, adding the specified chain
// as the last ones in the mutation flow.
func (c Chain) Extend(chain Chain) Chain {
	return c.Append(chain.hooks...)
}
