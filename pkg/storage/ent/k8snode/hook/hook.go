// Code generated by ent, DO NOT EDIT.

package hook

import (
	"context"
	"fmt"

	"danny.vn/hotpot/pkg/storage/ent/k8snode"
)

// The InventoryK8sNodeFunc type is an adapter to allow the use of ordinary
// function as InventoryK8sNode mutator.
type InventoryK8sNodeFunc func(context.Context, *k8snode.InventoryK8sNodeMutation) (k8snode.Value, error)

// Mutate calls f(ctx, m).
func (f InventoryK8sNodeFunc) Mutate(ctx context.Context, m k8snode.Mutation) (k8snode.Value, error) {
	if mv, ok := m.(*k8snode.InventoryK8sNodeMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *k8snode.InventoryK8sNodeMutation", m)
}

// The InventoryK8sNodeBronzeLinkFunc type is an adapter to allow the use of ordinary
// function as InventoryK8sNodeBronzeLink mutator.
type InventoryK8sNodeBronzeLinkFunc func(context.Context, *k8snode.InventoryK8sNodeBronzeLinkMutation) (k8snode.Value, error)

// Mutate calls f(ctx, m).
func (f InventoryK8sNodeBronzeLinkFunc) Mutate(ctx context.Context, m k8snode.Mutation) (k8snode.Value, error) {
	if mv, ok := m.(*k8snode.InventoryK8sNodeBronzeLinkMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *k8snode.InventoryK8sNodeBronzeLinkMutation", m)
}

// The InventoryK8sNodeNormalizedFunc type is an adapter to allow the use of ordinary
// function as InventoryK8sNodeNormalized mutator.
type InventoryK8sNodeNormalizedFunc func(context.Context, *k8snode.InventoryK8sNodeNormalizedMutation) (k8snode.Value, error)

// Mutate calls f(ctx, m).
func (f InventoryK8sNodeNormalizedFunc) Mutate(ctx context.Context, m k8snode.Mutation) (k8snode.Value, error) {
	if mv, ok := m.(*k8snode.InventoryK8sNodeNormalizedMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *k8snode.InventoryK8sNodeNormalizedMutation", m)
}

// Condition is a hook condition function.
type Condition func(context.Context, k8snode.Mutation) bool

// And groups conditions with the AND operator.
func And(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m k8snode.Mutation) bool {
		if !first(ctx, m) || !second(ctx, m) {
			return false
		}
		for _, cond := range rest {
			if !cond(ctx, m) {
				return false
			}
		}
		return true
	}
}

// Or groups conditions with the OR operator.
func Or(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m k8snode.Mutation) bool {
		if first(ctx, m) || second(ctx, m) {
			return true
		}
		for _, cond := range rest {
			if cond(ctx, m) {
				return true
			}
		}
		return false
	}
}

// Not negates a given condition.
func Not(cond Condition) Condition {
	return func(ctx context.Context, m k8snode.Mutation) bool {
		return !cond(ctx, m)
	}
}

// HasOp is a condition testing mutation operation.
func HasOp(op k8snode.Op) Condition {
	return func(_ context.Context, m k8snode.Mutation) bool {
		return m.Op().Is(op)
	}
}

// HasAddedFields is a condition validating `.AddedField` on fields.
func HasAddedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m k8snode.Mutation) bool {
		if _, exists := m.AddedField(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.AddedField(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasClearedFields is a condition validating `.FieldCleared` on fields.
func HasClearedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m k8snode.Mutation) bool {
		if exists := m.FieldCleared(field); !exists {
			return false
		}
		for _, field := range fields {
			if exists := m.FieldCleared(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasFields is a condition validating `.Field` on fields.
func HasFields(field string, fields ...string) Condition {
	return func(_ context.Context, m k8snode.Mutation) bool {
		if _, exists := m.Field(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.Field(field); !exists {
				return false
			}
		}
		return true
	}
}

// If executes the given hook under condition.
//
//	hook.If(ComputeAverage, And(HasFields(...), HasAddedFields(...)))
func If(hk k8snode.Hook, cond Condition) k8snode.Hook {
	return func(next k8snode.Mutator) k8snode.Mutator {
		return k8snode.MutateFunc(func(ctx context.Context, m k8snode.Mutation) (k8snode.Value, error) {
			if cond(ctx, m) {
				return hk(next).Mutate(ctx, m)
			}
			return next.Mutate(ctx, m)
		})
	}
}

// On executes the given hook only for the given operation.
//
//	hook.On(Log, k8snode.Delete|k8snode.Create)
func On(hk k8snode.Hook, op k8snode.Op) k8snode.Hook {
	return If(hk, HasOp(op))
}

// Unless skips the given hook only for the given operation.
//
//	hook.Unless(Log, k8snode.Update|k8snode.UpdateOne)
func Unless(hk k8snode.Hook, op k8snode.Op) k8snode.Hook {
	return If(hk, Not(HasOp(op)))
}

// FixedError is a hook returning a fixed error.
func FixedError(err error) k8snode.Hook {
	return func(k8snode.Mutator) k8snode.Mutator {
		return k8snode.MutateFunc(func(context.Context, k8snode.Mutation) (k8snode.Value, error) {
			return nil, err
		})
	}
}

// Reject returns a hook that rejects all operations that match op.
//
//	func (T) Hooks() []k8snode.Hook {
//		return []k8snode.Hook{
//			Reject(k8snode.Delete|k8snode.Update),
//		}
//	}
func Reject(op k8snode.Op) k8snode.Hook {
	hk := FixedError(fmt.Errorf("%s operation is not allowed", op))
	return On(hk, op)
}

// Chain acts as a list of hooks and is effectively immutable.
// Once created, it will always hold the same set of hooks in the same order.
type Chain struct {
	hooks []k8snode.Hook
}

// NewChain creates a new chain of hooks.
func NewChain(hooks ...k8snode.Hook) Chain {
	return Chain{append([]k8snode.Hook(nil), hooks...)}
}

// Hook chains the list of hooks and returns the final hook.
func (c Chain) Hook() k8snode.Hook {
	return func(mutator k8snode.Mutator) k8snode.Mutator {
		for i := len(c.hooks) - 1; i >= 0; i-- {
			mutator = c.hooks[i](mutator)
		}
		return mutator
	}
}

// Append extends a chain, adding the specified hook
// as the last ones in the mutation flow.
func (c Chain) Append(hooks ...k8snode.Hook) Chain {
	newHooks := make([]k8snode.Hook, 0, len(c.hooks)+len(hooks))
	newHooks = append(newHooks, c.hooks...)
	newHooks = append(newHooks, hooks...)
	return Chain{newHooks}
}

// Extend extends a chain, adding the specified chain
// as the last ones in the mutation flow.
func (c Chain) Extend(chain Chain) Chain {
	return c.Append(chain.hooks...)
}
