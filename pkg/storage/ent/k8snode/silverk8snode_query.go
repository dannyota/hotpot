// Code generated by ent, DO NOT EDIT.

package k8snode

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/dannyota/hotpot/pkg/storage/ent/k8snode/internal"
	"github.com/dannyota/hotpot/pkg/storage/ent/k8snode/predicate"
	"github.com/dannyota/hotpot/pkg/storage/ent/k8snode/silverk8snode"
	"github.com/dannyota/hotpot/pkg/storage/ent/k8snode/silverk8snodebronzelink"
)

// SilverK8sNodeQuery is the builder for querying SilverK8sNode entities.
type SilverK8sNodeQuery struct {
	config
	ctx             *QueryContext
	order           []silverk8snode.OrderOption
	inters          []Interceptor
	predicates      []predicate.SilverK8sNode
	withBronzeLinks *SilverK8sNodeBronzeLinkQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SilverK8sNodeQuery builder.
func (_q *SilverK8sNodeQuery) Where(ps ...predicate.SilverK8sNode) *SilverK8sNodeQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *SilverK8sNodeQuery) Limit(limit int) *SilverK8sNodeQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *SilverK8sNodeQuery) Offset(offset int) *SilverK8sNodeQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *SilverK8sNodeQuery) Unique(unique bool) *SilverK8sNodeQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *SilverK8sNodeQuery) Order(o ...silverk8snode.OrderOption) *SilverK8sNodeQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryBronzeLinks chains the current query on the "bronze_links" edge.
func (_q *SilverK8sNodeQuery) QueryBronzeLinks() *SilverK8sNodeBronzeLinkQuery {
	query := (&SilverK8sNodeBronzeLinkClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(silverk8snode.Table, silverk8snode.FieldID, selector),
			sqlgraph.To(silverk8snodebronzelink.Table, silverk8snodebronzelink.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, silverk8snode.BronzeLinksTable, silverk8snode.BronzeLinksColumn),
		)
		schemaConfig := _q.schemaConfig
		step.To.Schema = schemaConfig.SilverK8sNodeBronzeLink
		step.Edge.Schema = schemaConfig.SilverK8sNodeBronzeLink
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first SilverK8sNode entity from the query.
// Returns a *NotFoundError when no SilverK8sNode was found.
func (_q *SilverK8sNodeQuery) First(ctx context.Context) (*SilverK8sNode, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{silverk8snode.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *SilverK8sNodeQuery) FirstX(ctx context.Context) *SilverK8sNode {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SilverK8sNode ID from the query.
// Returns a *NotFoundError when no SilverK8sNode ID was found.
func (_q *SilverK8sNodeQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{silverk8snode.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *SilverK8sNodeQuery) FirstIDX(ctx context.Context) string {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SilverK8sNode entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SilverK8sNode entity is found.
// Returns a *NotFoundError when no SilverK8sNode entities are found.
func (_q *SilverK8sNodeQuery) Only(ctx context.Context) (*SilverK8sNode, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{silverk8snode.Label}
	default:
		return nil, &NotSingularError{silverk8snode.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *SilverK8sNodeQuery) OnlyX(ctx context.Context) *SilverK8sNode {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SilverK8sNode ID in the query.
// Returns a *NotSingularError when more than one SilverK8sNode ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *SilverK8sNodeQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{silverk8snode.Label}
	default:
		err = &NotSingularError{silverk8snode.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *SilverK8sNodeQuery) OnlyIDX(ctx context.Context) string {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SilverK8sNodes.
func (_q *SilverK8sNodeQuery) All(ctx context.Context) ([]*SilverK8sNode, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SilverK8sNode, *SilverK8sNodeQuery]()
	return withInterceptors[[]*SilverK8sNode](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *SilverK8sNodeQuery) AllX(ctx context.Context) []*SilverK8sNode {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SilverK8sNode IDs.
func (_q *SilverK8sNodeQuery) IDs(ctx context.Context) (ids []string, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(silverk8snode.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *SilverK8sNodeQuery) IDsX(ctx context.Context) []string {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *SilverK8sNodeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*SilverK8sNodeQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *SilverK8sNodeQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *SilverK8sNodeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("k8snode: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *SilverK8sNodeQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SilverK8sNodeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *SilverK8sNodeQuery) Clone() *SilverK8sNodeQuery {
	if _q == nil {
		return nil
	}
	return &SilverK8sNodeQuery{
		config:          _q.config,
		ctx:             _q.ctx.Clone(),
		order:           append([]silverk8snode.OrderOption{}, _q.order...),
		inters:          append([]Interceptor{}, _q.inters...),
		predicates:      append([]predicate.SilverK8sNode{}, _q.predicates...),
		withBronzeLinks: _q.withBronzeLinks.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithBronzeLinks tells the query-builder to eager-load the nodes that are connected to
// the "bronze_links" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SilverK8sNodeQuery) WithBronzeLinks(opts ...func(*SilverK8sNodeBronzeLinkQuery)) *SilverK8sNodeQuery {
	query := (&SilverK8sNodeBronzeLinkClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withBronzeLinks = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CollectedAt time.Time `json:"collected_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SilverK8sNode.Query().
//		GroupBy(silverk8snode.FieldCollectedAt).
//		Aggregate(k8snode.Count()).
//		Scan(ctx, &v)
func (_q *SilverK8sNodeQuery) GroupBy(field string, fields ...string) *SilverK8sNodeGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SilverK8sNodeGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = silverk8snode.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CollectedAt time.Time `json:"collected_at,omitempty"`
//	}
//
//	client.SilverK8sNode.Query().
//		Select(silverk8snode.FieldCollectedAt).
//		Scan(ctx, &v)
func (_q *SilverK8sNodeQuery) Select(fields ...string) *SilverK8sNodeSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &SilverK8sNodeSelect{SilverK8sNodeQuery: _q}
	sbuild.label = silverk8snode.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SilverK8sNodeSelect configured with the given aggregations.
func (_q *SilverK8sNodeQuery) Aggregate(fns ...AggregateFunc) *SilverK8sNodeSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *SilverK8sNodeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("k8snode: uninitialized interceptor (forgotten import k8snode/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !silverk8snode.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("k8snode: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *SilverK8sNodeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SilverK8sNode, error) {
	var (
		nodes       = []*SilverK8sNode{}
		_spec       = _q.querySpec()
		loadedTypes = [1]bool{
			_q.withBronzeLinks != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SilverK8sNode).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SilverK8sNode{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	_spec.Node.Schema = _q.schemaConfig.SilverK8sNode
	ctx = internal.NewSchemaConfigContext(ctx, _q.schemaConfig)
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withBronzeLinks; query != nil {
		if err := _q.loadBronzeLinks(ctx, query, nodes,
			func(n *SilverK8sNode) { n.Edges.BronzeLinks = []*SilverK8sNodeBronzeLink{} },
			func(n *SilverK8sNode, e *SilverK8sNodeBronzeLink) {
				n.Edges.BronzeLinks = append(n.Edges.BronzeLinks, e)
			}); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *SilverK8sNodeQuery) loadBronzeLinks(ctx context.Context, query *SilverK8sNodeBronzeLinkQuery, nodes []*SilverK8sNode, init func(*SilverK8sNode), assign func(*SilverK8sNode, *SilverK8sNodeBronzeLink)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*SilverK8sNode)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.SilverK8sNodeBronzeLink(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(silverk8snode.BronzeLinksColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.silver_k8s_node_bronze_links
		if fk == nil {
			return fmt.Errorf(`foreign-key "silver_k8s_node_bronze_links" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "silver_k8s_node_bronze_links" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *SilverK8sNodeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Schema = _q.schemaConfig.SilverK8sNode
	ctx = internal.NewSchemaConfigContext(ctx, _q.schemaConfig)
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *SilverK8sNodeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(silverk8snode.Table, silverk8snode.Columns, sqlgraph.NewFieldSpec(silverk8snode.FieldID, field.TypeString))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, silverk8snode.FieldID)
		for i := range fields {
			if fields[i] != silverk8snode.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *SilverK8sNodeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(silverk8snode.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = silverk8snode.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	t1.Schema(_q.schemaConfig.SilverK8sNode)
	ctx = internal.NewSchemaConfigContext(ctx, _q.schemaConfig)
	selector.WithContext(ctx)
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SilverK8sNodeGroupBy is the group-by builder for SilverK8sNode entities.
type SilverK8sNodeGroupBy struct {
	selector
	build *SilverK8sNodeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *SilverK8sNodeGroupBy) Aggregate(fns ...AggregateFunc) *SilverK8sNodeGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *SilverK8sNodeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SilverK8sNodeQuery, *SilverK8sNodeGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *SilverK8sNodeGroupBy) sqlScan(ctx context.Context, root *SilverK8sNodeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SilverK8sNodeSelect is the builder for selecting fields of SilverK8sNode entities.
type SilverK8sNodeSelect struct {
	*SilverK8sNodeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *SilverK8sNodeSelect) Aggregate(fns ...AggregateFunc) *SilverK8sNodeSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *SilverK8sNodeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SilverK8sNodeQuery, *SilverK8sNodeSelect](ctx, _s.SilverK8sNodeQuery, _s, _s.inters, v)
}

func (_s *SilverK8sNodeSelect) sqlScan(ctx context.Context, root *SilverK8sNodeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
