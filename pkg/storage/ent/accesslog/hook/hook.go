// Code generated by ent, DO NOT EDIT.

package hook

import (
	"context"
	"fmt"

	"danny.vn/hotpot/pkg/storage/ent/accesslog"
)

// The BronzeAccesslogClientIpFunc type is an adapter to allow the use of ordinary
// function as BronzeAccesslogClientIp mutator.
type BronzeAccesslogClientIpFunc func(context.Context, *accesslog.BronzeAccesslogClientIpMutation) (accesslog.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeAccesslogClientIpFunc) Mutate(ctx context.Context, m accesslog.Mutation) (accesslog.Value, error) {
	if mv, ok := m.(*accesslog.BronzeAccesslogClientIpMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *accesslog.BronzeAccesslogClientIpMutation", m)
}

// The BronzeAccesslogHttpCountFunc type is an adapter to allow the use of ordinary
// function as BronzeAccesslogHttpCount mutator.
type BronzeAccesslogHttpCountFunc func(context.Context, *accesslog.BronzeAccesslogHttpCountMutation) (accesslog.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeAccesslogHttpCountFunc) Mutate(ctx context.Context, m accesslog.Mutation) (accesslog.Value, error) {
	if mv, ok := m.(*accesslog.BronzeAccesslogHttpCountMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *accesslog.BronzeAccesslogHttpCountMutation", m)
}

// The BronzeAccesslogIngestCursorFunc type is an adapter to allow the use of ordinary
// function as BronzeAccesslogIngestCursor mutator.
type BronzeAccesslogIngestCursorFunc func(context.Context, *accesslog.BronzeAccesslogIngestCursorMutation) (accesslog.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeAccesslogIngestCursorFunc) Mutate(ctx context.Context, m accesslog.Mutation) (accesslog.Value, error) {
	if mv, ok := m.(*accesslog.BronzeAccesslogIngestCursorMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *accesslog.BronzeAccesslogIngestCursorMutation", m)
}

// The BronzeAccesslogUserAgentFunc type is an adapter to allow the use of ordinary
// function as BronzeAccesslogUserAgent mutator.
type BronzeAccesslogUserAgentFunc func(context.Context, *accesslog.BronzeAccesslogUserAgentMutation) (accesslog.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeAccesslogUserAgentFunc) Mutate(ctx context.Context, m accesslog.Mutation) (accesslog.Value, error) {
	if mv, ok := m.(*accesslog.BronzeAccesslogUserAgentMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *accesslog.BronzeAccesslogUserAgentMutation", m)
}

// Condition is a hook condition function.
type Condition func(context.Context, accesslog.Mutation) bool

// And groups conditions with the AND operator.
func And(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m accesslog.Mutation) bool {
		if !first(ctx, m) || !second(ctx, m) {
			return false
		}
		for _, cond := range rest {
			if !cond(ctx, m) {
				return false
			}
		}
		return true
	}
}

// Or groups conditions with the OR operator.
func Or(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m accesslog.Mutation) bool {
		if first(ctx, m) || second(ctx, m) {
			return true
		}
		for _, cond := range rest {
			if cond(ctx, m) {
				return true
			}
		}
		return false
	}
}

// Not negates a given condition.
func Not(cond Condition) Condition {
	return func(ctx context.Context, m accesslog.Mutation) bool {
		return !cond(ctx, m)
	}
}

// HasOp is a condition testing mutation operation.
func HasOp(op accesslog.Op) Condition {
	return func(_ context.Context, m accesslog.Mutation) bool {
		return m.Op().Is(op)
	}
}

// HasAddedFields is a condition validating `.AddedField` on fields.
func HasAddedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m accesslog.Mutation) bool {
		if _, exists := m.AddedField(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.AddedField(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasClearedFields is a condition validating `.FieldCleared` on fields.
func HasClearedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m accesslog.Mutation) bool {
		if exists := m.FieldCleared(field); !exists {
			return false
		}
		for _, field := range fields {
			if exists := m.FieldCleared(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasFields is a condition validating `.Field` on fields.
func HasFields(field string, fields ...string) Condition {
	return func(_ context.Context, m accesslog.Mutation) bool {
		if _, exists := m.Field(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.Field(field); !exists {
				return false
			}
		}
		return true
	}
}

// If executes the given hook under condition.
//
//	hook.If(ComputeAverage, And(HasFields(...), HasAddedFields(...)))
func If(hk accesslog.Hook, cond Condition) accesslog.Hook {
	return func(next accesslog.Mutator) accesslog.Mutator {
		return accesslog.MutateFunc(func(ctx context.Context, m accesslog.Mutation) (accesslog.Value, error) {
			if cond(ctx, m) {
				return hk(next).Mutate(ctx, m)
			}
			return next.Mutate(ctx, m)
		})
	}
}

// On executes the given hook only for the given operation.
//
//	hook.On(Log, accesslog.Delete|accesslog.Create)
func On(hk accesslog.Hook, op accesslog.Op) accesslog.Hook {
	return If(hk, HasOp(op))
}

// Unless skips the given hook only for the given operation.
//
//	hook.Unless(Log, accesslog.Update|accesslog.UpdateOne)
func Unless(hk accesslog.Hook, op accesslog.Op) accesslog.Hook {
	return If(hk, Not(HasOp(op)))
}

// FixedError is a hook returning a fixed error.
func FixedError(err error) accesslog.Hook {
	return func(accesslog.Mutator) accesslog.Mutator {
		return accesslog.MutateFunc(func(context.Context, accesslog.Mutation) (accesslog.Value, error) {
			return nil, err
		})
	}
}

// Reject returns a hook that rejects all operations that match op.
//
//	func (T) Hooks() []accesslog.Hook {
//		return []accesslog.Hook{
//			Reject(accesslog.Delete|accesslog.Update),
//		}
//	}
func Reject(op accesslog.Op) accesslog.Hook {
	hk := FixedError(fmt.Errorf("%s operation is not allowed", op))
	return On(hk, op)
}

// Chain acts as a list of hooks and is effectively immutable.
// Once created, it will always hold the same set of hooks in the same order.
type Chain struct {
	hooks []accesslog.Hook
}

// NewChain creates a new chain of hooks.
func NewChain(hooks ...accesslog.Hook) Chain {
	return Chain{append([]accesslog.Hook(nil), hooks...)}
}

// Hook chains the list of hooks and returns the final hook.
func (c Chain) Hook() accesslog.Hook {
	return func(mutator accesslog.Mutator) accesslog.Mutator {
		for i := len(c.hooks) - 1; i >= 0; i-- {
			mutator = c.hooks[i](mutator)
		}
		return mutator
	}
}

// Append extends a chain, adding the specified hook
// as the last ones in the mutation flow.
func (c Chain) Append(hooks ...accesslog.Hook) Chain {
	newHooks := make([]accesslog.Hook, 0, len(c.hooks)+len(hooks))
	newHooks = append(newHooks, c.hooks...)
	newHooks = append(newHooks, hooks...)
	return Chain{newHooks}
}

// Extend extends a chain, adding the specified chain
// as the last ones in the mutation flow.
func (c Chain) Extend(chain Chain) Chain {
	return c.Append(chain.hooks...)
}
