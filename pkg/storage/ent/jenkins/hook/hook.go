// Code generated by ent, DO NOT EDIT.

package hook

import (
	"context"
	"fmt"

	"danny.vn/hotpot/pkg/storage/ent/jenkins"
)

// The BronzeHistoryJenkinsBuildFunc type is an adapter to allow the use of ordinary
// function as BronzeHistoryJenkinsBuild mutator.
type BronzeHistoryJenkinsBuildFunc func(context.Context, *jenkins.BronzeHistoryJenkinsBuildMutation) (jenkins.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeHistoryJenkinsBuildFunc) Mutate(ctx context.Context, m jenkins.Mutation) (jenkins.Value, error) {
	if mv, ok := m.(*jenkins.BronzeHistoryJenkinsBuildMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *jenkins.BronzeHistoryJenkinsBuildMutation", m)
}

// The BronzeHistoryJenkinsBuildRepoFunc type is an adapter to allow the use of ordinary
// function as BronzeHistoryJenkinsBuildRepo mutator.
type BronzeHistoryJenkinsBuildRepoFunc func(context.Context, *jenkins.BronzeHistoryJenkinsBuildRepoMutation) (jenkins.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeHistoryJenkinsBuildRepoFunc) Mutate(ctx context.Context, m jenkins.Mutation) (jenkins.Value, error) {
	if mv, ok := m.(*jenkins.BronzeHistoryJenkinsBuildRepoMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *jenkins.BronzeHistoryJenkinsBuildRepoMutation", m)
}

// The BronzeHistoryJenkinsJobFunc type is an adapter to allow the use of ordinary
// function as BronzeHistoryJenkinsJob mutator.
type BronzeHistoryJenkinsJobFunc func(context.Context, *jenkins.BronzeHistoryJenkinsJobMutation) (jenkins.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeHistoryJenkinsJobFunc) Mutate(ctx context.Context, m jenkins.Mutation) (jenkins.Value, error) {
	if mv, ok := m.(*jenkins.BronzeHistoryJenkinsJobMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *jenkins.BronzeHistoryJenkinsJobMutation", m)
}

// The BronzeJenkinsBuildFunc type is an adapter to allow the use of ordinary
// function as BronzeJenkinsBuild mutator.
type BronzeJenkinsBuildFunc func(context.Context, *jenkins.BronzeJenkinsBuildMutation) (jenkins.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeJenkinsBuildFunc) Mutate(ctx context.Context, m jenkins.Mutation) (jenkins.Value, error) {
	if mv, ok := m.(*jenkins.BronzeJenkinsBuildMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *jenkins.BronzeJenkinsBuildMutation", m)
}

// The BronzeJenkinsBuildRepoFunc type is an adapter to allow the use of ordinary
// function as BronzeJenkinsBuildRepo mutator.
type BronzeJenkinsBuildRepoFunc func(context.Context, *jenkins.BronzeJenkinsBuildRepoMutation) (jenkins.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeJenkinsBuildRepoFunc) Mutate(ctx context.Context, m jenkins.Mutation) (jenkins.Value, error) {
	if mv, ok := m.(*jenkins.BronzeJenkinsBuildRepoMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *jenkins.BronzeJenkinsBuildRepoMutation", m)
}

// The BronzeJenkinsJobFunc type is an adapter to allow the use of ordinary
// function as BronzeJenkinsJob mutator.
type BronzeJenkinsJobFunc func(context.Context, *jenkins.BronzeJenkinsJobMutation) (jenkins.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeJenkinsJobFunc) Mutate(ctx context.Context, m jenkins.Mutation) (jenkins.Value, error) {
	if mv, ok := m.(*jenkins.BronzeJenkinsJobMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *jenkins.BronzeJenkinsJobMutation", m)
}

// Condition is a hook condition function.
type Condition func(context.Context, jenkins.Mutation) bool

// And groups conditions with the AND operator.
func And(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m jenkins.Mutation) bool {
		if !first(ctx, m) || !second(ctx, m) {
			return false
		}
		for _, cond := range rest {
			if !cond(ctx, m) {
				return false
			}
		}
		return true
	}
}

// Or groups conditions with the OR operator.
func Or(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m jenkins.Mutation) bool {
		if first(ctx, m) || second(ctx, m) {
			return true
		}
		for _, cond := range rest {
			if cond(ctx, m) {
				return true
			}
		}
		return false
	}
}

// Not negates a given condition.
func Not(cond Condition) Condition {
	return func(ctx context.Context, m jenkins.Mutation) bool {
		return !cond(ctx, m)
	}
}

// HasOp is a condition testing mutation operation.
func HasOp(op jenkins.Op) Condition {
	return func(_ context.Context, m jenkins.Mutation) bool {
		return m.Op().Is(op)
	}
}

// HasAddedFields is a condition validating `.AddedField` on fields.
func HasAddedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m jenkins.Mutation) bool {
		if _, exists := m.AddedField(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.AddedField(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasClearedFields is a condition validating `.FieldCleared` on fields.
func HasClearedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m jenkins.Mutation) bool {
		if exists := m.FieldCleared(field); !exists {
			return false
		}
		for _, field := range fields {
			if exists := m.FieldCleared(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasFields is a condition validating `.Field` on fields.
func HasFields(field string, fields ...string) Condition {
	return func(_ context.Context, m jenkins.Mutation) bool {
		if _, exists := m.Field(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.Field(field); !exists {
				return false
			}
		}
		return true
	}
}

// If executes the given hook under condition.
//
//	hook.If(ComputeAverage, And(HasFields(...), HasAddedFields(...)))
func If(hk jenkins.Hook, cond Condition) jenkins.Hook {
	return func(next jenkins.Mutator) jenkins.Mutator {
		return jenkins.MutateFunc(func(ctx context.Context, m jenkins.Mutation) (jenkins.Value, error) {
			if cond(ctx, m) {
				return hk(next).Mutate(ctx, m)
			}
			return next.Mutate(ctx, m)
		})
	}
}

// On executes the given hook only for the given operation.
//
//	hook.On(Log, jenkins.Delete|jenkins.Create)
func On(hk jenkins.Hook, op jenkins.Op) jenkins.Hook {
	return If(hk, HasOp(op))
}

// Unless skips the given hook only for the given operation.
//
//	hook.Unless(Log, jenkins.Update|jenkins.UpdateOne)
func Unless(hk jenkins.Hook, op jenkins.Op) jenkins.Hook {
	return If(hk, Not(HasOp(op)))
}

// FixedError is a hook returning a fixed error.
func FixedError(err error) jenkins.Hook {
	return func(jenkins.Mutator) jenkins.Mutator {
		return jenkins.MutateFunc(func(context.Context, jenkins.Mutation) (jenkins.Value, error) {
			return nil, err
		})
	}
}

// Reject returns a hook that rejects all operations that match op.
//
//	func (T) Hooks() []jenkins.Hook {
//		return []jenkins.Hook{
//			Reject(jenkins.Delete|jenkins.Update),
//		}
//	}
func Reject(op jenkins.Op) jenkins.Hook {
	hk := FixedError(fmt.Errorf("%s operation is not allowed", op))
	return On(hk, op)
}

// Chain acts as a list of hooks and is effectively immutable.
// Once created, it will always hold the same set of hooks in the same order.
type Chain struct {
	hooks []jenkins.Hook
}

// NewChain creates a new chain of hooks.
func NewChain(hooks ...jenkins.Hook) Chain {
	return Chain{append([]jenkins.Hook(nil), hooks...)}
}

// Hook chains the list of hooks and returns the final hook.
func (c Chain) Hook() jenkins.Hook {
	return func(mutator jenkins.Mutator) jenkins.Mutator {
		for i := len(c.hooks) - 1; i >= 0; i-- {
			mutator = c.hooks[i](mutator)
		}
		return mutator
	}
}

// Append extends a chain, adding the specified hook
// as the last ones in the mutation flow.
func (c Chain) Append(hooks ...jenkins.Hook) Chain {
	newHooks := make([]jenkins.Hook, 0, len(c.hooks)+len(hooks))
	newHooks = append(newHooks, c.hooks...)
	newHooks = append(newHooks, hooks...)
	return Chain{newHooks}
}

// Extend extends a chain, adding the specified chain
// as the last ones in the mutation flow.
func (c Chain) Extend(chain Chain) Chain {
	return c.Append(chain.hooks...)
}
