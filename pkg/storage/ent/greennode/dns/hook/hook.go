// Code generated by ent, DO NOT EDIT.

package hook

import (
	"context"
	"fmt"

	"danny.vn/hotpot/pkg/storage/ent/greennode/dns"
)

// The BronzeGreenNodeDNSHostedZoneFunc type is an adapter to allow the use of ordinary
// function as BronzeGreenNodeDNSHostedZone mutator.
type BronzeGreenNodeDNSHostedZoneFunc func(context.Context, *dns.BronzeGreenNodeDNSHostedZoneMutation) (dns.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeGreenNodeDNSHostedZoneFunc) Mutate(ctx context.Context, m dns.Mutation) (dns.Value, error) {
	if mv, ok := m.(*dns.BronzeGreenNodeDNSHostedZoneMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *dns.BronzeGreenNodeDNSHostedZoneMutation", m)
}

// The BronzeGreenNodeDNSRecordFunc type is an adapter to allow the use of ordinary
// function as BronzeGreenNodeDNSRecord mutator.
type BronzeGreenNodeDNSRecordFunc func(context.Context, *dns.BronzeGreenNodeDNSRecordMutation) (dns.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeGreenNodeDNSRecordFunc) Mutate(ctx context.Context, m dns.Mutation) (dns.Value, error) {
	if mv, ok := m.(*dns.BronzeGreenNodeDNSRecordMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *dns.BronzeGreenNodeDNSRecordMutation", m)
}

// The BronzeHistoryGreenNodeDNSHostedZoneFunc type is an adapter to allow the use of ordinary
// function as BronzeHistoryGreenNodeDNSHostedZone mutator.
type BronzeHistoryGreenNodeDNSHostedZoneFunc func(context.Context, *dns.BronzeHistoryGreenNodeDNSHostedZoneMutation) (dns.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeHistoryGreenNodeDNSHostedZoneFunc) Mutate(ctx context.Context, m dns.Mutation) (dns.Value, error) {
	if mv, ok := m.(*dns.BronzeHistoryGreenNodeDNSHostedZoneMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *dns.BronzeHistoryGreenNodeDNSHostedZoneMutation", m)
}

// The BronzeHistoryGreenNodeDNSRecordFunc type is an adapter to allow the use of ordinary
// function as BronzeHistoryGreenNodeDNSRecord mutator.
type BronzeHistoryGreenNodeDNSRecordFunc func(context.Context, *dns.BronzeHistoryGreenNodeDNSRecordMutation) (dns.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeHistoryGreenNodeDNSRecordFunc) Mutate(ctx context.Context, m dns.Mutation) (dns.Value, error) {
	if mv, ok := m.(*dns.BronzeHistoryGreenNodeDNSRecordMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *dns.BronzeHistoryGreenNodeDNSRecordMutation", m)
}

// Condition is a hook condition function.
type Condition func(context.Context, dns.Mutation) bool

// And groups conditions with the AND operator.
func And(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m dns.Mutation) bool {
		if !first(ctx, m) || !second(ctx, m) {
			return false
		}
		for _, cond := range rest {
			if !cond(ctx, m) {
				return false
			}
		}
		return true
	}
}

// Or groups conditions with the OR operator.
func Or(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m dns.Mutation) bool {
		if first(ctx, m) || second(ctx, m) {
			return true
		}
		for _, cond := range rest {
			if cond(ctx, m) {
				return true
			}
		}
		return false
	}
}

// Not negates a given condition.
func Not(cond Condition) Condition {
	return func(ctx context.Context, m dns.Mutation) bool {
		return !cond(ctx, m)
	}
}

// HasOp is a condition testing mutation operation.
func HasOp(op dns.Op) Condition {
	return func(_ context.Context, m dns.Mutation) bool {
		return m.Op().Is(op)
	}
}

// HasAddedFields is a condition validating `.AddedField` on fields.
func HasAddedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m dns.Mutation) bool {
		if _, exists := m.AddedField(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.AddedField(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasClearedFields is a condition validating `.FieldCleared` on fields.
func HasClearedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m dns.Mutation) bool {
		if exists := m.FieldCleared(field); !exists {
			return false
		}
		for _, field := range fields {
			if exists := m.FieldCleared(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasFields is a condition validating `.Field` on fields.
func HasFields(field string, fields ...string) Condition {
	return func(_ context.Context, m dns.Mutation) bool {
		if _, exists := m.Field(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.Field(field); !exists {
				return false
			}
		}
		return true
	}
}

// If executes the given hook under condition.
//
//	hook.If(ComputeAverage, And(HasFields(...), HasAddedFields(...)))
func If(hk dns.Hook, cond Condition) dns.Hook {
	return func(next dns.Mutator) dns.Mutator {
		return dns.MutateFunc(func(ctx context.Context, m dns.Mutation) (dns.Value, error) {
			if cond(ctx, m) {
				return hk(next).Mutate(ctx, m)
			}
			return next.Mutate(ctx, m)
		})
	}
}

// On executes the given hook only for the given operation.
//
//	hook.On(Log, dns.Delete|dns.Create)
func On(hk dns.Hook, op dns.Op) dns.Hook {
	return If(hk, HasOp(op))
}

// Unless skips the given hook only for the given operation.
//
//	hook.Unless(Log, dns.Update|dns.UpdateOne)
func Unless(hk dns.Hook, op dns.Op) dns.Hook {
	return If(hk, Not(HasOp(op)))
}

// FixedError is a hook returning a fixed error.
func FixedError(err error) dns.Hook {
	return func(dns.Mutator) dns.Mutator {
		return dns.MutateFunc(func(context.Context, dns.Mutation) (dns.Value, error) {
			return nil, err
		})
	}
}

// Reject returns a hook that rejects all operations that match op.
//
//	func (T) Hooks() []dns.Hook {
//		return []dns.Hook{
//			Reject(dns.Delete|dns.Update),
//		}
//	}
func Reject(op dns.Op) dns.Hook {
	hk := FixedError(fmt.Errorf("%s operation is not allowed", op))
	return On(hk, op)
}

// Chain acts as a list of hooks and is effectively immutable.
// Once created, it will always hold the same set of hooks in the same order.
type Chain struct {
	hooks []dns.Hook
}

// NewChain creates a new chain of hooks.
func NewChain(hooks ...dns.Hook) Chain {
	return Chain{append([]dns.Hook(nil), hooks...)}
}

// Hook chains the list of hooks and returns the final hook.
func (c Chain) Hook() dns.Hook {
	return func(mutator dns.Mutator) dns.Mutator {
		for i := len(c.hooks) - 1; i >= 0; i-- {
			mutator = c.hooks[i](mutator)
		}
		return mutator
	}
}

// Append extends a chain, adding the specified hook
// as the last ones in the mutation flow.
func (c Chain) Append(hooks ...dns.Hook) Chain {
	newHooks := make([]dns.Hook, 0, len(c.hooks)+len(hooks))
	newHooks = append(newHooks, c.hooks...)
	newHooks = append(newHooks, hooks...)
	return Chain{newHooks}
}

// Extend extends a chain, adding the specified chain
// as the last ones in the mutation flow.
func (c Chain) Extend(chain Chain) Chain {
	return c.Append(chain.hooks...)
}
