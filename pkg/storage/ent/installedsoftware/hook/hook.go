// Code generated by ent, DO NOT EDIT.

package hook

import (
	"context"
	"fmt"

	"github.com/dannyota/hotpot/pkg/storage/ent/installedsoftware"
)

// The SilverInstalledSoftwareFunc type is an adapter to allow the use of ordinary
// function as SilverInstalledSoftware mutator.
type SilverInstalledSoftwareFunc func(context.Context, *installedsoftware.SilverInstalledSoftwareMutation) (installedsoftware.Value, error)

// Mutate calls f(ctx, m).
func (f SilverInstalledSoftwareFunc) Mutate(ctx context.Context, m installedsoftware.Mutation) (installedsoftware.Value, error) {
	if mv, ok := m.(*installedsoftware.SilverInstalledSoftwareMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *installedsoftware.SilverInstalledSoftwareMutation", m)
}

// The SilverInstalledSoftwareBronzeLinkFunc type is an adapter to allow the use of ordinary
// function as SilverInstalledSoftwareBronzeLink mutator.
type SilverInstalledSoftwareBronzeLinkFunc func(context.Context, *installedsoftware.SilverInstalledSoftwareBronzeLinkMutation) (installedsoftware.Value, error)

// Mutate calls f(ctx, m).
func (f SilverInstalledSoftwareBronzeLinkFunc) Mutate(ctx context.Context, m installedsoftware.Mutation) (installedsoftware.Value, error) {
	if mv, ok := m.(*installedsoftware.SilverInstalledSoftwareBronzeLinkMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *installedsoftware.SilverInstalledSoftwareBronzeLinkMutation", m)
}

// The SilverInstalledSoftwareNormalizedFunc type is an adapter to allow the use of ordinary
// function as SilverInstalledSoftwareNormalized mutator.
type SilverInstalledSoftwareNormalizedFunc func(context.Context, *installedsoftware.SilverInstalledSoftwareNormalizedMutation) (installedsoftware.Value, error)

// Mutate calls f(ctx, m).
func (f SilverInstalledSoftwareNormalizedFunc) Mutate(ctx context.Context, m installedsoftware.Mutation) (installedsoftware.Value, error) {
	if mv, ok := m.(*installedsoftware.SilverInstalledSoftwareNormalizedMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *installedsoftware.SilverInstalledSoftwareNormalizedMutation", m)
}

// Condition is a hook condition function.
type Condition func(context.Context, installedsoftware.Mutation) bool

// And groups conditions with the AND operator.
func And(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m installedsoftware.Mutation) bool {
		if !first(ctx, m) || !second(ctx, m) {
			return false
		}
		for _, cond := range rest {
			if !cond(ctx, m) {
				return false
			}
		}
		return true
	}
}

// Or groups conditions with the OR operator.
func Or(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m installedsoftware.Mutation) bool {
		if first(ctx, m) || second(ctx, m) {
			return true
		}
		for _, cond := range rest {
			if cond(ctx, m) {
				return true
			}
		}
		return false
	}
}

// Not negates a given condition.
func Not(cond Condition) Condition {
	return func(ctx context.Context, m installedsoftware.Mutation) bool {
		return !cond(ctx, m)
	}
}

// HasOp is a condition testing mutation operation.
func HasOp(op installedsoftware.Op) Condition {
	return func(_ context.Context, m installedsoftware.Mutation) bool {
		return m.Op().Is(op)
	}
}

// HasAddedFields is a condition validating `.AddedField` on fields.
func HasAddedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m installedsoftware.Mutation) bool {
		if _, exists := m.AddedField(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.AddedField(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasClearedFields is a condition validating `.FieldCleared` on fields.
func HasClearedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m installedsoftware.Mutation) bool {
		if exists := m.FieldCleared(field); !exists {
			return false
		}
		for _, field := range fields {
			if exists := m.FieldCleared(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasFields is a condition validating `.Field` on fields.
func HasFields(field string, fields ...string) Condition {
	return func(_ context.Context, m installedsoftware.Mutation) bool {
		if _, exists := m.Field(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.Field(field); !exists {
				return false
			}
		}
		return true
	}
}

// If executes the given hook under condition.
//
//	hook.If(ComputeAverage, And(HasFields(...), HasAddedFields(...)))
func If(hk installedsoftware.Hook, cond Condition) installedsoftware.Hook {
	return func(next installedsoftware.Mutator) installedsoftware.Mutator {
		return installedsoftware.MutateFunc(func(ctx context.Context, m installedsoftware.Mutation) (installedsoftware.Value, error) {
			if cond(ctx, m) {
				return hk(next).Mutate(ctx, m)
			}
			return next.Mutate(ctx, m)
		})
	}
}

// On executes the given hook only for the given operation.
//
//	hook.On(Log, installedsoftware.Delete|installedsoftware.Create)
func On(hk installedsoftware.Hook, op installedsoftware.Op) installedsoftware.Hook {
	return If(hk, HasOp(op))
}

// Unless skips the given hook only for the given operation.
//
//	hook.Unless(Log, installedsoftware.Update|installedsoftware.UpdateOne)
func Unless(hk installedsoftware.Hook, op installedsoftware.Op) installedsoftware.Hook {
	return If(hk, Not(HasOp(op)))
}

// FixedError is a hook returning a fixed error.
func FixedError(err error) installedsoftware.Hook {
	return func(installedsoftware.Mutator) installedsoftware.Mutator {
		return installedsoftware.MutateFunc(func(context.Context, installedsoftware.Mutation) (installedsoftware.Value, error) {
			return nil, err
		})
	}
}

// Reject returns a hook that rejects all operations that match op.
//
//	func (T) Hooks() []installedsoftware.Hook {
//		return []installedsoftware.Hook{
//			Reject(installedsoftware.Delete|installedsoftware.Update),
//		}
//	}
func Reject(op installedsoftware.Op) installedsoftware.Hook {
	hk := FixedError(fmt.Errorf("%s operation is not allowed", op))
	return On(hk, op)
}

// Chain acts as a list of hooks and is effectively immutable.
// Once created, it will always hold the same set of hooks in the same order.
type Chain struct {
	hooks []installedsoftware.Hook
}

// NewChain creates a new chain of hooks.
func NewChain(hooks ...installedsoftware.Hook) Chain {
	return Chain{append([]installedsoftware.Hook(nil), hooks...)}
}

// Hook chains the list of hooks and returns the final hook.
func (c Chain) Hook() installedsoftware.Hook {
	return func(mutator installedsoftware.Mutator) installedsoftware.Mutator {
		for i := len(c.hooks) - 1; i >= 0; i-- {
			mutator = c.hooks[i](mutator)
		}
		return mutator
	}
}

// Append extends a chain, adding the specified hook
// as the last ones in the mutation flow.
func (c Chain) Append(hooks ...installedsoftware.Hook) Chain {
	newHooks := make([]installedsoftware.Hook, 0, len(c.hooks)+len(hooks))
	newHooks = append(newHooks, c.hooks...)
	newHooks = append(newHooks, hooks...)
	return Chain{newHooks}
}

// Extend extends a chain, adding the specified chain
// as the last ones in the mutation flow.
func (c Chain) Extend(chain Chain) Chain {
	return c.Append(chain.hooks...)
}
