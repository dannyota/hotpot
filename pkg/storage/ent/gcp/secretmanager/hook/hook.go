// Code generated by ent, DO NOT EDIT.

package hook

import (
	"context"
	"fmt"

	"danny.vn/hotpot/pkg/storage/ent/gcp/secretmanager"
)

// The BronzeGCPSecretManagerSecretFunc type is an adapter to allow the use of ordinary
// function as BronzeGCPSecretManagerSecret mutator.
type BronzeGCPSecretManagerSecretFunc func(context.Context, *secretmanager.BronzeGCPSecretManagerSecretMutation) (secretmanager.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeGCPSecretManagerSecretFunc) Mutate(ctx context.Context, m secretmanager.Mutation) (secretmanager.Value, error) {
	if mv, ok := m.(*secretmanager.BronzeGCPSecretManagerSecretMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *secretmanager.BronzeGCPSecretManagerSecretMutation", m)
}

// The BronzeGCPSecretManagerSecretLabelFunc type is an adapter to allow the use of ordinary
// function as BronzeGCPSecretManagerSecretLabel mutator.
type BronzeGCPSecretManagerSecretLabelFunc func(context.Context, *secretmanager.BronzeGCPSecretManagerSecretLabelMutation) (secretmanager.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeGCPSecretManagerSecretLabelFunc) Mutate(ctx context.Context, m secretmanager.Mutation) (secretmanager.Value, error) {
	if mv, ok := m.(*secretmanager.BronzeGCPSecretManagerSecretLabelMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *secretmanager.BronzeGCPSecretManagerSecretLabelMutation", m)
}

// The BronzeHistoryGCPSecretManagerSecretFunc type is an adapter to allow the use of ordinary
// function as BronzeHistoryGCPSecretManagerSecret mutator.
type BronzeHistoryGCPSecretManagerSecretFunc func(context.Context, *secretmanager.BronzeHistoryGCPSecretManagerSecretMutation) (secretmanager.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeHistoryGCPSecretManagerSecretFunc) Mutate(ctx context.Context, m secretmanager.Mutation) (secretmanager.Value, error) {
	if mv, ok := m.(*secretmanager.BronzeHistoryGCPSecretManagerSecretMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *secretmanager.BronzeHistoryGCPSecretManagerSecretMutation", m)
}

// The BronzeHistoryGCPSecretManagerSecretLabelFunc type is an adapter to allow the use of ordinary
// function as BronzeHistoryGCPSecretManagerSecretLabel mutator.
type BronzeHistoryGCPSecretManagerSecretLabelFunc func(context.Context, *secretmanager.BronzeHistoryGCPSecretManagerSecretLabelMutation) (secretmanager.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeHistoryGCPSecretManagerSecretLabelFunc) Mutate(ctx context.Context, m secretmanager.Mutation) (secretmanager.Value, error) {
	if mv, ok := m.(*secretmanager.BronzeHistoryGCPSecretManagerSecretLabelMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *secretmanager.BronzeHistoryGCPSecretManagerSecretLabelMutation", m)
}

// Condition is a hook condition function.
type Condition func(context.Context, secretmanager.Mutation) bool

// And groups conditions with the AND operator.
func And(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m secretmanager.Mutation) bool {
		if !first(ctx, m) || !second(ctx, m) {
			return false
		}
		for _, cond := range rest {
			if !cond(ctx, m) {
				return false
			}
		}
		return true
	}
}

// Or groups conditions with the OR operator.
func Or(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m secretmanager.Mutation) bool {
		if first(ctx, m) || second(ctx, m) {
			return true
		}
		for _, cond := range rest {
			if cond(ctx, m) {
				return true
			}
		}
		return false
	}
}

// Not negates a given condition.
func Not(cond Condition) Condition {
	return func(ctx context.Context, m secretmanager.Mutation) bool {
		return !cond(ctx, m)
	}
}

// HasOp is a condition testing mutation operation.
func HasOp(op secretmanager.Op) Condition {
	return func(_ context.Context, m secretmanager.Mutation) bool {
		return m.Op().Is(op)
	}
}

// HasAddedFields is a condition validating `.AddedField` on fields.
func HasAddedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m secretmanager.Mutation) bool {
		if _, exists := m.AddedField(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.AddedField(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasClearedFields is a condition validating `.FieldCleared` on fields.
func HasClearedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m secretmanager.Mutation) bool {
		if exists := m.FieldCleared(field); !exists {
			return false
		}
		for _, field := range fields {
			if exists := m.FieldCleared(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasFields is a condition validating `.Field` on fields.
func HasFields(field string, fields ...string) Condition {
	return func(_ context.Context, m secretmanager.Mutation) bool {
		if _, exists := m.Field(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.Field(field); !exists {
				return false
			}
		}
		return true
	}
}

// If executes the given hook under condition.
//
//	hook.If(ComputeAverage, And(HasFields(...), HasAddedFields(...)))
func If(hk secretmanager.Hook, cond Condition) secretmanager.Hook {
	return func(next secretmanager.Mutator) secretmanager.Mutator {
		return secretmanager.MutateFunc(func(ctx context.Context, m secretmanager.Mutation) (secretmanager.Value, error) {
			if cond(ctx, m) {
				return hk(next).Mutate(ctx, m)
			}
			return next.Mutate(ctx, m)
		})
	}
}

// On executes the given hook only for the given operation.
//
//	hook.On(Log, secretmanager.Delete|secretmanager.Create)
func On(hk secretmanager.Hook, op secretmanager.Op) secretmanager.Hook {
	return If(hk, HasOp(op))
}

// Unless skips the given hook only for the given operation.
//
//	hook.Unless(Log, secretmanager.Update|secretmanager.UpdateOne)
func Unless(hk secretmanager.Hook, op secretmanager.Op) secretmanager.Hook {
	return If(hk, Not(HasOp(op)))
}

// FixedError is a hook returning a fixed error.
func FixedError(err error) secretmanager.Hook {
	return func(secretmanager.Mutator) secretmanager.Mutator {
		return secretmanager.MutateFunc(func(context.Context, secretmanager.Mutation) (secretmanager.Value, error) {
			return nil, err
		})
	}
}

// Reject returns a hook that rejects all operations that match op.
//
//	func (T) Hooks() []secretmanager.Hook {
//		return []secretmanager.Hook{
//			Reject(secretmanager.Delete|secretmanager.Update),
//		}
//	}
func Reject(op secretmanager.Op) secretmanager.Hook {
	hk := FixedError(fmt.Errorf("%s operation is not allowed", op))
	return On(hk, op)
}

// Chain acts as a list of hooks and is effectively immutable.
// Once created, it will always hold the same set of hooks in the same order.
type Chain struct {
	hooks []secretmanager.Hook
}

// NewChain creates a new chain of hooks.
func NewChain(hooks ...secretmanager.Hook) Chain {
	return Chain{append([]secretmanager.Hook(nil), hooks...)}
}

// Hook chains the list of hooks and returns the final hook.
func (c Chain) Hook() secretmanager.Hook {
	return func(mutator secretmanager.Mutator) secretmanager.Mutator {
		for i := len(c.hooks) - 1; i >= 0; i-- {
			mutator = c.hooks[i](mutator)
		}
		return mutator
	}
}

// Append extends a chain, adding the specified hook
// as the last ones in the mutation flow.
func (c Chain) Append(hooks ...secretmanager.Hook) Chain {
	newHooks := make([]secretmanager.Hook, 0, len(c.hooks)+len(hooks))
	newHooks = append(newHooks, c.hooks...)
	newHooks = append(newHooks, hooks...)
	return Chain{newHooks}
}

// Extend extends a chain, adding the specified chain
// as the last ones in the mutation flow.
func (c Chain) Extend(chain Chain) Chain {
	return c.Append(chain.hooks...)
}
