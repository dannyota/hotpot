// Code generated by ent, DO NOT EDIT.

package hook

import (
	"context"
	"fmt"

	"danny.vn/hotpot/pkg/storage/ent/gcp/binaryauthorization"
)

// The BronzeGCPBinaryAuthorizationAttestorFunc type is an adapter to allow the use of ordinary
// function as BronzeGCPBinaryAuthorizationAttestor mutator.
type BronzeGCPBinaryAuthorizationAttestorFunc func(context.Context, *binaryauthorization.BronzeGCPBinaryAuthorizationAttestorMutation) (binaryauthorization.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeGCPBinaryAuthorizationAttestorFunc) Mutate(ctx context.Context, m binaryauthorization.Mutation) (binaryauthorization.Value, error) {
	if mv, ok := m.(*binaryauthorization.BronzeGCPBinaryAuthorizationAttestorMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *binaryauthorization.BronzeGCPBinaryAuthorizationAttestorMutation", m)
}

// The BronzeGCPBinaryAuthorizationPolicyFunc type is an adapter to allow the use of ordinary
// function as BronzeGCPBinaryAuthorizationPolicy mutator.
type BronzeGCPBinaryAuthorizationPolicyFunc func(context.Context, *binaryauthorization.BronzeGCPBinaryAuthorizationPolicyMutation) (binaryauthorization.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeGCPBinaryAuthorizationPolicyFunc) Mutate(ctx context.Context, m binaryauthorization.Mutation) (binaryauthorization.Value, error) {
	if mv, ok := m.(*binaryauthorization.BronzeGCPBinaryAuthorizationPolicyMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *binaryauthorization.BronzeGCPBinaryAuthorizationPolicyMutation", m)
}

// The BronzeHistoryGCPBinaryAuthorizationAttestorFunc type is an adapter to allow the use of ordinary
// function as BronzeHistoryGCPBinaryAuthorizationAttestor mutator.
type BronzeHistoryGCPBinaryAuthorizationAttestorFunc func(context.Context, *binaryauthorization.BronzeHistoryGCPBinaryAuthorizationAttestorMutation) (binaryauthorization.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeHistoryGCPBinaryAuthorizationAttestorFunc) Mutate(ctx context.Context, m binaryauthorization.Mutation) (binaryauthorization.Value, error) {
	if mv, ok := m.(*binaryauthorization.BronzeHistoryGCPBinaryAuthorizationAttestorMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *binaryauthorization.BronzeHistoryGCPBinaryAuthorizationAttestorMutation", m)
}

// The BronzeHistoryGCPBinaryAuthorizationPolicyFunc type is an adapter to allow the use of ordinary
// function as BronzeHistoryGCPBinaryAuthorizationPolicy mutator.
type BronzeHistoryGCPBinaryAuthorizationPolicyFunc func(context.Context, *binaryauthorization.BronzeHistoryGCPBinaryAuthorizationPolicyMutation) (binaryauthorization.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeHistoryGCPBinaryAuthorizationPolicyFunc) Mutate(ctx context.Context, m binaryauthorization.Mutation) (binaryauthorization.Value, error) {
	if mv, ok := m.(*binaryauthorization.BronzeHistoryGCPBinaryAuthorizationPolicyMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *binaryauthorization.BronzeHistoryGCPBinaryAuthorizationPolicyMutation", m)
}

// Condition is a hook condition function.
type Condition func(context.Context, binaryauthorization.Mutation) bool

// And groups conditions with the AND operator.
func And(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m binaryauthorization.Mutation) bool {
		if !first(ctx, m) || !second(ctx, m) {
			return false
		}
		for _, cond := range rest {
			if !cond(ctx, m) {
				return false
			}
		}
		return true
	}
}

// Or groups conditions with the OR operator.
func Or(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m binaryauthorization.Mutation) bool {
		if first(ctx, m) || second(ctx, m) {
			return true
		}
		for _, cond := range rest {
			if cond(ctx, m) {
				return true
			}
		}
		return false
	}
}

// Not negates a given condition.
func Not(cond Condition) Condition {
	return func(ctx context.Context, m binaryauthorization.Mutation) bool {
		return !cond(ctx, m)
	}
}

// HasOp is a condition testing mutation operation.
func HasOp(op binaryauthorization.Op) Condition {
	return func(_ context.Context, m binaryauthorization.Mutation) bool {
		return m.Op().Is(op)
	}
}

// HasAddedFields is a condition validating `.AddedField` on fields.
func HasAddedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m binaryauthorization.Mutation) bool {
		if _, exists := m.AddedField(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.AddedField(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasClearedFields is a condition validating `.FieldCleared` on fields.
func HasClearedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m binaryauthorization.Mutation) bool {
		if exists := m.FieldCleared(field); !exists {
			return false
		}
		for _, field := range fields {
			if exists := m.FieldCleared(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasFields is a condition validating `.Field` on fields.
func HasFields(field string, fields ...string) Condition {
	return func(_ context.Context, m binaryauthorization.Mutation) bool {
		if _, exists := m.Field(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.Field(field); !exists {
				return false
			}
		}
		return true
	}
}

// If executes the given hook under condition.
//
//	hook.If(ComputeAverage, And(HasFields(...), HasAddedFields(...)))
func If(hk binaryauthorization.Hook, cond Condition) binaryauthorization.Hook {
	return func(next binaryauthorization.Mutator) binaryauthorization.Mutator {
		return binaryauthorization.MutateFunc(func(ctx context.Context, m binaryauthorization.Mutation) (binaryauthorization.Value, error) {
			if cond(ctx, m) {
				return hk(next).Mutate(ctx, m)
			}
			return next.Mutate(ctx, m)
		})
	}
}

// On executes the given hook only for the given operation.
//
//	hook.On(Log, binaryauthorization.Delete|binaryauthorization.Create)
func On(hk binaryauthorization.Hook, op binaryauthorization.Op) binaryauthorization.Hook {
	return If(hk, HasOp(op))
}

// Unless skips the given hook only for the given operation.
//
//	hook.Unless(Log, binaryauthorization.Update|binaryauthorization.UpdateOne)
func Unless(hk binaryauthorization.Hook, op binaryauthorization.Op) binaryauthorization.Hook {
	return If(hk, Not(HasOp(op)))
}

// FixedError is a hook returning a fixed error.
func FixedError(err error) binaryauthorization.Hook {
	return func(binaryauthorization.Mutator) binaryauthorization.Mutator {
		return binaryauthorization.MutateFunc(func(context.Context, binaryauthorization.Mutation) (binaryauthorization.Value, error) {
			return nil, err
		})
	}
}

// Reject returns a hook that rejects all operations that match op.
//
//	func (T) Hooks() []binaryauthorization.Hook {
//		return []binaryauthorization.Hook{
//			Reject(binaryauthorization.Delete|binaryauthorization.Update),
//		}
//	}
func Reject(op binaryauthorization.Op) binaryauthorization.Hook {
	hk := FixedError(fmt.Errorf("%s operation is not allowed", op))
	return On(hk, op)
}

// Chain acts as a list of hooks and is effectively immutable.
// Once created, it will always hold the same set of hooks in the same order.
type Chain struct {
	hooks []binaryauthorization.Hook
}

// NewChain creates a new chain of hooks.
func NewChain(hooks ...binaryauthorization.Hook) Chain {
	return Chain{append([]binaryauthorization.Hook(nil), hooks...)}
}

// Hook chains the list of hooks and returns the final hook.
func (c Chain) Hook() binaryauthorization.Hook {
	return func(mutator binaryauthorization.Mutator) binaryauthorization.Mutator {
		for i := len(c.hooks) - 1; i >= 0; i-- {
			mutator = c.hooks[i](mutator)
		}
		return mutator
	}
}

// Append extends a chain, adding the specified hook
// as the last ones in the mutation flow.
func (c Chain) Append(hooks ...binaryauthorization.Hook) Chain {
	newHooks := make([]binaryauthorization.Hook, 0, len(c.hooks)+len(hooks))
	newHooks = append(newHooks, c.hooks...)
	newHooks = append(newHooks, hooks...)
	return Chain{newHooks}
}

// Extend extends a chain, adding the specified chain
// as the last ones in the mutation flow.
func (c Chain) Extend(chain Chain) Chain {
	return c.Append(chain.hooks...)
}
