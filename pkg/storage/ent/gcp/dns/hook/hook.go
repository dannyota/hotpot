// Code generated by ent, DO NOT EDIT.

package hook

import (
	"context"
	"fmt"

	"danny.vn/hotpot/pkg/storage/ent/gcp/dns"
)

// The BronzeGCPDNSManagedZoneFunc type is an adapter to allow the use of ordinary
// function as BronzeGCPDNSManagedZone mutator.
type BronzeGCPDNSManagedZoneFunc func(context.Context, *dns.BronzeGCPDNSManagedZoneMutation) (dns.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeGCPDNSManagedZoneFunc) Mutate(ctx context.Context, m dns.Mutation) (dns.Value, error) {
	if mv, ok := m.(*dns.BronzeGCPDNSManagedZoneMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *dns.BronzeGCPDNSManagedZoneMutation", m)
}

// The BronzeGCPDNSManagedZoneLabelFunc type is an adapter to allow the use of ordinary
// function as BronzeGCPDNSManagedZoneLabel mutator.
type BronzeGCPDNSManagedZoneLabelFunc func(context.Context, *dns.BronzeGCPDNSManagedZoneLabelMutation) (dns.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeGCPDNSManagedZoneLabelFunc) Mutate(ctx context.Context, m dns.Mutation) (dns.Value, error) {
	if mv, ok := m.(*dns.BronzeGCPDNSManagedZoneLabelMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *dns.BronzeGCPDNSManagedZoneLabelMutation", m)
}

// The BronzeGCPDNSPolicyFunc type is an adapter to allow the use of ordinary
// function as BronzeGCPDNSPolicy mutator.
type BronzeGCPDNSPolicyFunc func(context.Context, *dns.BronzeGCPDNSPolicyMutation) (dns.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeGCPDNSPolicyFunc) Mutate(ctx context.Context, m dns.Mutation) (dns.Value, error) {
	if mv, ok := m.(*dns.BronzeGCPDNSPolicyMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *dns.BronzeGCPDNSPolicyMutation", m)
}

// The BronzeHistoryGCPDNSManagedZoneFunc type is an adapter to allow the use of ordinary
// function as BronzeHistoryGCPDNSManagedZone mutator.
type BronzeHistoryGCPDNSManagedZoneFunc func(context.Context, *dns.BronzeHistoryGCPDNSManagedZoneMutation) (dns.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeHistoryGCPDNSManagedZoneFunc) Mutate(ctx context.Context, m dns.Mutation) (dns.Value, error) {
	if mv, ok := m.(*dns.BronzeHistoryGCPDNSManagedZoneMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *dns.BronzeHistoryGCPDNSManagedZoneMutation", m)
}

// The BronzeHistoryGCPDNSManagedZoneLabelFunc type is an adapter to allow the use of ordinary
// function as BronzeHistoryGCPDNSManagedZoneLabel mutator.
type BronzeHistoryGCPDNSManagedZoneLabelFunc func(context.Context, *dns.BronzeHistoryGCPDNSManagedZoneLabelMutation) (dns.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeHistoryGCPDNSManagedZoneLabelFunc) Mutate(ctx context.Context, m dns.Mutation) (dns.Value, error) {
	if mv, ok := m.(*dns.BronzeHistoryGCPDNSManagedZoneLabelMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *dns.BronzeHistoryGCPDNSManagedZoneLabelMutation", m)
}

// The BronzeHistoryGCPDNSPolicyFunc type is an adapter to allow the use of ordinary
// function as BronzeHistoryGCPDNSPolicy mutator.
type BronzeHistoryGCPDNSPolicyFunc func(context.Context, *dns.BronzeHistoryGCPDNSPolicyMutation) (dns.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeHistoryGCPDNSPolicyFunc) Mutate(ctx context.Context, m dns.Mutation) (dns.Value, error) {
	if mv, ok := m.(*dns.BronzeHistoryGCPDNSPolicyMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *dns.BronzeHistoryGCPDNSPolicyMutation", m)
}

// Condition is a hook condition function.
type Condition func(context.Context, dns.Mutation) bool

// And groups conditions with the AND operator.
func And(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m dns.Mutation) bool {
		if !first(ctx, m) || !second(ctx, m) {
			return false
		}
		for _, cond := range rest {
			if !cond(ctx, m) {
				return false
			}
		}
		return true
	}
}

// Or groups conditions with the OR operator.
func Or(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m dns.Mutation) bool {
		if first(ctx, m) || second(ctx, m) {
			return true
		}
		for _, cond := range rest {
			if cond(ctx, m) {
				return true
			}
		}
		return false
	}
}

// Not negates a given condition.
func Not(cond Condition) Condition {
	return func(ctx context.Context, m dns.Mutation) bool {
		return !cond(ctx, m)
	}
}

// HasOp is a condition testing mutation operation.
func HasOp(op dns.Op) Condition {
	return func(_ context.Context, m dns.Mutation) bool {
		return m.Op().Is(op)
	}
}

// HasAddedFields is a condition validating `.AddedField` on fields.
func HasAddedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m dns.Mutation) bool {
		if _, exists := m.AddedField(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.AddedField(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasClearedFields is a condition validating `.FieldCleared` on fields.
func HasClearedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m dns.Mutation) bool {
		if exists := m.FieldCleared(field); !exists {
			return false
		}
		for _, field := range fields {
			if exists := m.FieldCleared(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasFields is a condition validating `.Field` on fields.
func HasFields(field string, fields ...string) Condition {
	return func(_ context.Context, m dns.Mutation) bool {
		if _, exists := m.Field(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.Field(field); !exists {
				return false
			}
		}
		return true
	}
}

// If executes the given hook under condition.
//
//	hook.If(ComputeAverage, And(HasFields(...), HasAddedFields(...)))
func If(hk dns.Hook, cond Condition) dns.Hook {
	return func(next dns.Mutator) dns.Mutator {
		return dns.MutateFunc(func(ctx context.Context, m dns.Mutation) (dns.Value, error) {
			if cond(ctx, m) {
				return hk(next).Mutate(ctx, m)
			}
			return next.Mutate(ctx, m)
		})
	}
}

// On executes the given hook only for the given operation.
//
//	hook.On(Log, dns.Delete|dns.Create)
func On(hk dns.Hook, op dns.Op) dns.Hook {
	return If(hk, HasOp(op))
}

// Unless skips the given hook only for the given operation.
//
//	hook.Unless(Log, dns.Update|dns.UpdateOne)
func Unless(hk dns.Hook, op dns.Op) dns.Hook {
	return If(hk, Not(HasOp(op)))
}

// FixedError is a hook returning a fixed error.
func FixedError(err error) dns.Hook {
	return func(dns.Mutator) dns.Mutator {
		return dns.MutateFunc(func(context.Context, dns.Mutation) (dns.Value, error) {
			return nil, err
		})
	}
}

// Reject returns a hook that rejects all operations that match op.
//
//	func (T) Hooks() []dns.Hook {
//		return []dns.Hook{
//			Reject(dns.Delete|dns.Update),
//		}
//	}
func Reject(op dns.Op) dns.Hook {
	hk := FixedError(fmt.Errorf("%s operation is not allowed", op))
	return On(hk, op)
}

// Chain acts as a list of hooks and is effectively immutable.
// Once created, it will always hold the same set of hooks in the same order.
type Chain struct {
	hooks []dns.Hook
}

// NewChain creates a new chain of hooks.
func NewChain(hooks ...dns.Hook) Chain {
	return Chain{append([]dns.Hook(nil), hooks...)}
}

// Hook chains the list of hooks and returns the final hook.
func (c Chain) Hook() dns.Hook {
	return func(mutator dns.Mutator) dns.Mutator {
		for i := len(c.hooks) - 1; i >= 0; i-- {
			mutator = c.hooks[i](mutator)
		}
		return mutator
	}
}

// Append extends a chain, adding the specified hook
// as the last ones in the mutation flow.
func (c Chain) Append(hooks ...dns.Hook) Chain {
	newHooks := make([]dns.Hook, 0, len(c.hooks)+len(hooks))
	newHooks = append(newHooks, c.hooks...)
	newHooks = append(newHooks, hooks...)
	return Chain{newHooks}
}

// Extend extends a chain, adding the specified chain
// as the last ones in the mutation flow.
func (c Chain) Extend(chain Chain) Chain {
	return c.Append(chain.hooks...)
}
