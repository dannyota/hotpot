// Code generated by ent, DO NOT EDIT.

package hook

import (
	"context"
	"fmt"

	"github.com/dannyota/hotpot/pkg/storage/ent/gcp/pubsub"
)

// The BronzeGCPPubSubSubscriptionFunc type is an adapter to allow the use of ordinary
// function as BronzeGCPPubSubSubscription mutator.
type BronzeGCPPubSubSubscriptionFunc func(context.Context, *pubsub.BronzeGCPPubSubSubscriptionMutation) (pubsub.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeGCPPubSubSubscriptionFunc) Mutate(ctx context.Context, m pubsub.Mutation) (pubsub.Value, error) {
	if mv, ok := m.(*pubsub.BronzeGCPPubSubSubscriptionMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *pubsub.BronzeGCPPubSubSubscriptionMutation", m)
}

// The BronzeGCPPubSubTopicFunc type is an adapter to allow the use of ordinary
// function as BronzeGCPPubSubTopic mutator.
type BronzeGCPPubSubTopicFunc func(context.Context, *pubsub.BronzeGCPPubSubTopicMutation) (pubsub.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeGCPPubSubTopicFunc) Mutate(ctx context.Context, m pubsub.Mutation) (pubsub.Value, error) {
	if mv, ok := m.(*pubsub.BronzeGCPPubSubTopicMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *pubsub.BronzeGCPPubSubTopicMutation", m)
}

// The BronzeHistoryGCPPubSubSubscriptionFunc type is an adapter to allow the use of ordinary
// function as BronzeHistoryGCPPubSubSubscription mutator.
type BronzeHistoryGCPPubSubSubscriptionFunc func(context.Context, *pubsub.BronzeHistoryGCPPubSubSubscriptionMutation) (pubsub.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeHistoryGCPPubSubSubscriptionFunc) Mutate(ctx context.Context, m pubsub.Mutation) (pubsub.Value, error) {
	if mv, ok := m.(*pubsub.BronzeHistoryGCPPubSubSubscriptionMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *pubsub.BronzeHistoryGCPPubSubSubscriptionMutation", m)
}

// The BronzeHistoryGCPPubSubTopicFunc type is an adapter to allow the use of ordinary
// function as BronzeHistoryGCPPubSubTopic mutator.
type BronzeHistoryGCPPubSubTopicFunc func(context.Context, *pubsub.BronzeHistoryGCPPubSubTopicMutation) (pubsub.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeHistoryGCPPubSubTopicFunc) Mutate(ctx context.Context, m pubsub.Mutation) (pubsub.Value, error) {
	if mv, ok := m.(*pubsub.BronzeHistoryGCPPubSubTopicMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *pubsub.BronzeHistoryGCPPubSubTopicMutation", m)
}

// Condition is a hook condition function.
type Condition func(context.Context, pubsub.Mutation) bool

// And groups conditions with the AND operator.
func And(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m pubsub.Mutation) bool {
		if !first(ctx, m) || !second(ctx, m) {
			return false
		}
		for _, cond := range rest {
			if !cond(ctx, m) {
				return false
			}
		}
		return true
	}
}

// Or groups conditions with the OR operator.
func Or(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m pubsub.Mutation) bool {
		if first(ctx, m) || second(ctx, m) {
			return true
		}
		for _, cond := range rest {
			if cond(ctx, m) {
				return true
			}
		}
		return false
	}
}

// Not negates a given condition.
func Not(cond Condition) Condition {
	return func(ctx context.Context, m pubsub.Mutation) bool {
		return !cond(ctx, m)
	}
}

// HasOp is a condition testing mutation operation.
func HasOp(op pubsub.Op) Condition {
	return func(_ context.Context, m pubsub.Mutation) bool {
		return m.Op().Is(op)
	}
}

// HasAddedFields is a condition validating `.AddedField` on fields.
func HasAddedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m pubsub.Mutation) bool {
		if _, exists := m.AddedField(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.AddedField(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasClearedFields is a condition validating `.FieldCleared` on fields.
func HasClearedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m pubsub.Mutation) bool {
		if exists := m.FieldCleared(field); !exists {
			return false
		}
		for _, field := range fields {
			if exists := m.FieldCleared(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasFields is a condition validating `.Field` on fields.
func HasFields(field string, fields ...string) Condition {
	return func(_ context.Context, m pubsub.Mutation) bool {
		if _, exists := m.Field(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.Field(field); !exists {
				return false
			}
		}
		return true
	}
}

// If executes the given hook under condition.
//
//	hook.If(ComputeAverage, And(HasFields(...), HasAddedFields(...)))
func If(hk pubsub.Hook, cond Condition) pubsub.Hook {
	return func(next pubsub.Mutator) pubsub.Mutator {
		return pubsub.MutateFunc(func(ctx context.Context, m pubsub.Mutation) (pubsub.Value, error) {
			if cond(ctx, m) {
				return hk(next).Mutate(ctx, m)
			}
			return next.Mutate(ctx, m)
		})
	}
}

// On executes the given hook only for the given operation.
//
//	hook.On(Log, pubsub.Delete|pubsub.Create)
func On(hk pubsub.Hook, op pubsub.Op) pubsub.Hook {
	return If(hk, HasOp(op))
}

// Unless skips the given hook only for the given operation.
//
//	hook.Unless(Log, pubsub.Update|pubsub.UpdateOne)
func Unless(hk pubsub.Hook, op pubsub.Op) pubsub.Hook {
	return If(hk, Not(HasOp(op)))
}

// FixedError is a hook returning a fixed error.
func FixedError(err error) pubsub.Hook {
	return func(pubsub.Mutator) pubsub.Mutator {
		return pubsub.MutateFunc(func(context.Context, pubsub.Mutation) (pubsub.Value, error) {
			return nil, err
		})
	}
}

// Reject returns a hook that rejects all operations that match op.
//
//	func (T) Hooks() []pubsub.Hook {
//		return []pubsub.Hook{
//			Reject(pubsub.Delete|pubsub.Update),
//		}
//	}
func Reject(op pubsub.Op) pubsub.Hook {
	hk := FixedError(fmt.Errorf("%s operation is not allowed", op))
	return On(hk, op)
}

// Chain acts as a list of hooks and is effectively immutable.
// Once created, it will always hold the same set of hooks in the same order.
type Chain struct {
	hooks []pubsub.Hook
}

// NewChain creates a new chain of hooks.
func NewChain(hooks ...pubsub.Hook) Chain {
	return Chain{append([]pubsub.Hook(nil), hooks...)}
}

// Hook chains the list of hooks and returns the final hook.
func (c Chain) Hook() pubsub.Hook {
	return func(mutator pubsub.Mutator) pubsub.Mutator {
		for i := len(c.hooks) - 1; i >= 0; i-- {
			mutator = c.hooks[i](mutator)
		}
		return mutator
	}
}

// Append extends a chain, adding the specified hook
// as the last ones in the mutation flow.
func (c Chain) Append(hooks ...pubsub.Hook) Chain {
	newHooks := make([]pubsub.Hook, 0, len(c.hooks)+len(hooks))
	newHooks = append(newHooks, c.hooks...)
	newHooks = append(newHooks, hooks...)
	return Chain{newHooks}
}

// Extend extends a chain, adding the specified chain
// as the last ones in the mutation flow.
func (c Chain) Extend(chain Chain) Chain {
	return c.Append(chain.hooks...)
}
