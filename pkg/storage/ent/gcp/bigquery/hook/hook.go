// Code generated by ent, DO NOT EDIT.

package hook

import (
	"context"
	"fmt"

	"danny.vn/hotpot/pkg/storage/ent/gcp/bigquery"
)

// The BronzeGCPBigQueryDatasetFunc type is an adapter to allow the use of ordinary
// function as BronzeGCPBigQueryDataset mutator.
type BronzeGCPBigQueryDatasetFunc func(context.Context, *bigquery.BronzeGCPBigQueryDatasetMutation) (bigquery.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeGCPBigQueryDatasetFunc) Mutate(ctx context.Context, m bigquery.Mutation) (bigquery.Value, error) {
	if mv, ok := m.(*bigquery.BronzeGCPBigQueryDatasetMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *bigquery.BronzeGCPBigQueryDatasetMutation", m)
}

// The BronzeGCPBigQueryTableFunc type is an adapter to allow the use of ordinary
// function as BronzeGCPBigQueryTable mutator.
type BronzeGCPBigQueryTableFunc func(context.Context, *bigquery.BronzeGCPBigQueryTableMutation) (bigquery.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeGCPBigQueryTableFunc) Mutate(ctx context.Context, m bigquery.Mutation) (bigquery.Value, error) {
	if mv, ok := m.(*bigquery.BronzeGCPBigQueryTableMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *bigquery.BronzeGCPBigQueryTableMutation", m)
}

// The BronzeHistoryGCPBigQueryDatasetFunc type is an adapter to allow the use of ordinary
// function as BronzeHistoryGCPBigQueryDataset mutator.
type BronzeHistoryGCPBigQueryDatasetFunc func(context.Context, *bigquery.BronzeHistoryGCPBigQueryDatasetMutation) (bigquery.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeHistoryGCPBigQueryDatasetFunc) Mutate(ctx context.Context, m bigquery.Mutation) (bigquery.Value, error) {
	if mv, ok := m.(*bigquery.BronzeHistoryGCPBigQueryDatasetMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *bigquery.BronzeHistoryGCPBigQueryDatasetMutation", m)
}

// The BronzeHistoryGCPBigQueryTableFunc type is an adapter to allow the use of ordinary
// function as BronzeHistoryGCPBigQueryTable mutator.
type BronzeHistoryGCPBigQueryTableFunc func(context.Context, *bigquery.BronzeHistoryGCPBigQueryTableMutation) (bigquery.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeHistoryGCPBigQueryTableFunc) Mutate(ctx context.Context, m bigquery.Mutation) (bigquery.Value, error) {
	if mv, ok := m.(*bigquery.BronzeHistoryGCPBigQueryTableMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *bigquery.BronzeHistoryGCPBigQueryTableMutation", m)
}

// Condition is a hook condition function.
type Condition func(context.Context, bigquery.Mutation) bool

// And groups conditions with the AND operator.
func And(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m bigquery.Mutation) bool {
		if !first(ctx, m) || !second(ctx, m) {
			return false
		}
		for _, cond := range rest {
			if !cond(ctx, m) {
				return false
			}
		}
		return true
	}
}

// Or groups conditions with the OR operator.
func Or(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m bigquery.Mutation) bool {
		if first(ctx, m) || second(ctx, m) {
			return true
		}
		for _, cond := range rest {
			if cond(ctx, m) {
				return true
			}
		}
		return false
	}
}

// Not negates a given condition.
func Not(cond Condition) Condition {
	return func(ctx context.Context, m bigquery.Mutation) bool {
		return !cond(ctx, m)
	}
}

// HasOp is a condition testing mutation operation.
func HasOp(op bigquery.Op) Condition {
	return func(_ context.Context, m bigquery.Mutation) bool {
		return m.Op().Is(op)
	}
}

// HasAddedFields is a condition validating `.AddedField` on fields.
func HasAddedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m bigquery.Mutation) bool {
		if _, exists := m.AddedField(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.AddedField(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasClearedFields is a condition validating `.FieldCleared` on fields.
func HasClearedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m bigquery.Mutation) bool {
		if exists := m.FieldCleared(field); !exists {
			return false
		}
		for _, field := range fields {
			if exists := m.FieldCleared(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasFields is a condition validating `.Field` on fields.
func HasFields(field string, fields ...string) Condition {
	return func(_ context.Context, m bigquery.Mutation) bool {
		if _, exists := m.Field(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.Field(field); !exists {
				return false
			}
		}
		return true
	}
}

// If executes the given hook under condition.
//
//	hook.If(ComputeAverage, And(HasFields(...), HasAddedFields(...)))
func If(hk bigquery.Hook, cond Condition) bigquery.Hook {
	return func(next bigquery.Mutator) bigquery.Mutator {
		return bigquery.MutateFunc(func(ctx context.Context, m bigquery.Mutation) (bigquery.Value, error) {
			if cond(ctx, m) {
				return hk(next).Mutate(ctx, m)
			}
			return next.Mutate(ctx, m)
		})
	}
}

// On executes the given hook only for the given operation.
//
//	hook.On(Log, bigquery.Delete|bigquery.Create)
func On(hk bigquery.Hook, op bigquery.Op) bigquery.Hook {
	return If(hk, HasOp(op))
}

// Unless skips the given hook only for the given operation.
//
//	hook.Unless(Log, bigquery.Update|bigquery.UpdateOne)
func Unless(hk bigquery.Hook, op bigquery.Op) bigquery.Hook {
	return If(hk, Not(HasOp(op)))
}

// FixedError is a hook returning a fixed error.
func FixedError(err error) bigquery.Hook {
	return func(bigquery.Mutator) bigquery.Mutator {
		return bigquery.MutateFunc(func(context.Context, bigquery.Mutation) (bigquery.Value, error) {
			return nil, err
		})
	}
}

// Reject returns a hook that rejects all operations that match op.
//
//	func (T) Hooks() []bigquery.Hook {
//		return []bigquery.Hook{
//			Reject(bigquery.Delete|bigquery.Update),
//		}
//	}
func Reject(op bigquery.Op) bigquery.Hook {
	hk := FixedError(fmt.Errorf("%s operation is not allowed", op))
	return On(hk, op)
}

// Chain acts as a list of hooks and is effectively immutable.
// Once created, it will always hold the same set of hooks in the same order.
type Chain struct {
	hooks []bigquery.Hook
}

// NewChain creates a new chain of hooks.
func NewChain(hooks ...bigquery.Hook) Chain {
	return Chain{append([]bigquery.Hook(nil), hooks...)}
}

// Hook chains the list of hooks and returns the final hook.
func (c Chain) Hook() bigquery.Hook {
	return func(mutator bigquery.Mutator) bigquery.Mutator {
		for i := len(c.hooks) - 1; i >= 0; i-- {
			mutator = c.hooks[i](mutator)
		}
		return mutator
	}
}

// Append extends a chain, adding the specified hook
// as the last ones in the mutation flow.
func (c Chain) Append(hooks ...bigquery.Hook) Chain {
	newHooks := make([]bigquery.Hook, 0, len(c.hooks)+len(hooks))
	newHooks = append(newHooks, c.hooks...)
	newHooks = append(newHooks, hooks...)
	return Chain{newHooks}
}

// Extend extends a chain, adding the specified chain
// as the last ones in the mutation flow.
func (c Chain) Extend(chain Chain) Chain {
	return c.Append(chain.hooks...)
}
