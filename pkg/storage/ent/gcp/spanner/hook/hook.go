// Code generated by ent, DO NOT EDIT.

package hook

import (
	"context"
	"fmt"

	"github.com/dannyota/hotpot/pkg/storage/ent/gcp/spanner"
)

// The BronzeGCPSpannerDatabaseFunc type is an adapter to allow the use of ordinary
// function as BronzeGCPSpannerDatabase mutator.
type BronzeGCPSpannerDatabaseFunc func(context.Context, *spanner.BronzeGCPSpannerDatabaseMutation) (spanner.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeGCPSpannerDatabaseFunc) Mutate(ctx context.Context, m spanner.Mutation) (spanner.Value, error) {
	if mv, ok := m.(*spanner.BronzeGCPSpannerDatabaseMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *spanner.BronzeGCPSpannerDatabaseMutation", m)
}

// The BronzeGCPSpannerInstanceFunc type is an adapter to allow the use of ordinary
// function as BronzeGCPSpannerInstance mutator.
type BronzeGCPSpannerInstanceFunc func(context.Context, *spanner.BronzeGCPSpannerInstanceMutation) (spanner.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeGCPSpannerInstanceFunc) Mutate(ctx context.Context, m spanner.Mutation) (spanner.Value, error) {
	if mv, ok := m.(*spanner.BronzeGCPSpannerInstanceMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *spanner.BronzeGCPSpannerInstanceMutation", m)
}

// The BronzeHistoryGCPSpannerDatabaseFunc type is an adapter to allow the use of ordinary
// function as BronzeHistoryGCPSpannerDatabase mutator.
type BronzeHistoryGCPSpannerDatabaseFunc func(context.Context, *spanner.BronzeHistoryGCPSpannerDatabaseMutation) (spanner.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeHistoryGCPSpannerDatabaseFunc) Mutate(ctx context.Context, m spanner.Mutation) (spanner.Value, error) {
	if mv, ok := m.(*spanner.BronzeHistoryGCPSpannerDatabaseMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *spanner.BronzeHistoryGCPSpannerDatabaseMutation", m)
}

// The BronzeHistoryGCPSpannerInstanceFunc type is an adapter to allow the use of ordinary
// function as BronzeHistoryGCPSpannerInstance mutator.
type BronzeHistoryGCPSpannerInstanceFunc func(context.Context, *spanner.BronzeHistoryGCPSpannerInstanceMutation) (spanner.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeHistoryGCPSpannerInstanceFunc) Mutate(ctx context.Context, m spanner.Mutation) (spanner.Value, error) {
	if mv, ok := m.(*spanner.BronzeHistoryGCPSpannerInstanceMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *spanner.BronzeHistoryGCPSpannerInstanceMutation", m)
}

// Condition is a hook condition function.
type Condition func(context.Context, spanner.Mutation) bool

// And groups conditions with the AND operator.
func And(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m spanner.Mutation) bool {
		if !first(ctx, m) || !second(ctx, m) {
			return false
		}
		for _, cond := range rest {
			if !cond(ctx, m) {
				return false
			}
		}
		return true
	}
}

// Or groups conditions with the OR operator.
func Or(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m spanner.Mutation) bool {
		if first(ctx, m) || second(ctx, m) {
			return true
		}
		for _, cond := range rest {
			if cond(ctx, m) {
				return true
			}
		}
		return false
	}
}

// Not negates a given condition.
func Not(cond Condition) Condition {
	return func(ctx context.Context, m spanner.Mutation) bool {
		return !cond(ctx, m)
	}
}

// HasOp is a condition testing mutation operation.
func HasOp(op spanner.Op) Condition {
	return func(_ context.Context, m spanner.Mutation) bool {
		return m.Op().Is(op)
	}
}

// HasAddedFields is a condition validating `.AddedField` on fields.
func HasAddedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m spanner.Mutation) bool {
		if _, exists := m.AddedField(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.AddedField(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasClearedFields is a condition validating `.FieldCleared` on fields.
func HasClearedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m spanner.Mutation) bool {
		if exists := m.FieldCleared(field); !exists {
			return false
		}
		for _, field := range fields {
			if exists := m.FieldCleared(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasFields is a condition validating `.Field` on fields.
func HasFields(field string, fields ...string) Condition {
	return func(_ context.Context, m spanner.Mutation) bool {
		if _, exists := m.Field(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.Field(field); !exists {
				return false
			}
		}
		return true
	}
}

// If executes the given hook under condition.
//
//	hook.If(ComputeAverage, And(HasFields(...), HasAddedFields(...)))
func If(hk spanner.Hook, cond Condition) spanner.Hook {
	return func(next spanner.Mutator) spanner.Mutator {
		return spanner.MutateFunc(func(ctx context.Context, m spanner.Mutation) (spanner.Value, error) {
			if cond(ctx, m) {
				return hk(next).Mutate(ctx, m)
			}
			return next.Mutate(ctx, m)
		})
	}
}

// On executes the given hook only for the given operation.
//
//	hook.On(Log, spanner.Delete|spanner.Create)
func On(hk spanner.Hook, op spanner.Op) spanner.Hook {
	return If(hk, HasOp(op))
}

// Unless skips the given hook only for the given operation.
//
//	hook.Unless(Log, spanner.Update|spanner.UpdateOne)
func Unless(hk spanner.Hook, op spanner.Op) spanner.Hook {
	return If(hk, Not(HasOp(op)))
}

// FixedError is a hook returning a fixed error.
func FixedError(err error) spanner.Hook {
	return func(spanner.Mutator) spanner.Mutator {
		return spanner.MutateFunc(func(context.Context, spanner.Mutation) (spanner.Value, error) {
			return nil, err
		})
	}
}

// Reject returns a hook that rejects all operations that match op.
//
//	func (T) Hooks() []spanner.Hook {
//		return []spanner.Hook{
//			Reject(spanner.Delete|spanner.Update),
//		}
//	}
func Reject(op spanner.Op) spanner.Hook {
	hk := FixedError(fmt.Errorf("%s operation is not allowed", op))
	return On(hk, op)
}

// Chain acts as a list of hooks and is effectively immutable.
// Once created, it will always hold the same set of hooks in the same order.
type Chain struct {
	hooks []spanner.Hook
}

// NewChain creates a new chain of hooks.
func NewChain(hooks ...spanner.Hook) Chain {
	return Chain{append([]spanner.Hook(nil), hooks...)}
}

// Hook chains the list of hooks and returns the final hook.
func (c Chain) Hook() spanner.Hook {
	return func(mutator spanner.Mutator) spanner.Mutator {
		for i := len(c.hooks) - 1; i >= 0; i-- {
			mutator = c.hooks[i](mutator)
		}
		return mutator
	}
}

// Append extends a chain, adding the specified hook
// as the last ones in the mutation flow.
func (c Chain) Append(hooks ...spanner.Hook) Chain {
	newHooks := make([]spanner.Hook, 0, len(c.hooks)+len(hooks))
	newHooks = append(newHooks, c.hooks...)
	newHooks = append(newHooks, hooks...)
	return Chain{newHooks}
}

// Extend extends a chain, adding the specified chain
// as the last ones in the mutation flow.
func (c Chain) Extend(chain Chain) Chain {
	return c.Append(chain.hooks...)
}
