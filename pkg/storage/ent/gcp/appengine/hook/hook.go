// Code generated by ent, DO NOT EDIT.

package hook

import (
	"context"
	"fmt"

	"danny.vn/hotpot/pkg/storage/ent/gcp/appengine"
)

// The BronzeGCPAppEngineApplicationFunc type is an adapter to allow the use of ordinary
// function as BronzeGCPAppEngineApplication mutator.
type BronzeGCPAppEngineApplicationFunc func(context.Context, *appengine.BronzeGCPAppEngineApplicationMutation) (appengine.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeGCPAppEngineApplicationFunc) Mutate(ctx context.Context, m appengine.Mutation) (appengine.Value, error) {
	if mv, ok := m.(*appengine.BronzeGCPAppEngineApplicationMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *appengine.BronzeGCPAppEngineApplicationMutation", m)
}

// The BronzeGCPAppEngineServiceFunc type is an adapter to allow the use of ordinary
// function as BronzeGCPAppEngineService mutator.
type BronzeGCPAppEngineServiceFunc func(context.Context, *appengine.BronzeGCPAppEngineServiceMutation) (appengine.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeGCPAppEngineServiceFunc) Mutate(ctx context.Context, m appengine.Mutation) (appengine.Value, error) {
	if mv, ok := m.(*appengine.BronzeGCPAppEngineServiceMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *appengine.BronzeGCPAppEngineServiceMutation", m)
}

// The BronzeHistoryGCPAppEngineApplicationFunc type is an adapter to allow the use of ordinary
// function as BronzeHistoryGCPAppEngineApplication mutator.
type BronzeHistoryGCPAppEngineApplicationFunc func(context.Context, *appengine.BronzeHistoryGCPAppEngineApplicationMutation) (appengine.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeHistoryGCPAppEngineApplicationFunc) Mutate(ctx context.Context, m appengine.Mutation) (appengine.Value, error) {
	if mv, ok := m.(*appengine.BronzeHistoryGCPAppEngineApplicationMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *appengine.BronzeHistoryGCPAppEngineApplicationMutation", m)
}

// The BronzeHistoryGCPAppEngineServiceFunc type is an adapter to allow the use of ordinary
// function as BronzeHistoryGCPAppEngineService mutator.
type BronzeHistoryGCPAppEngineServiceFunc func(context.Context, *appengine.BronzeHistoryGCPAppEngineServiceMutation) (appengine.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeHistoryGCPAppEngineServiceFunc) Mutate(ctx context.Context, m appengine.Mutation) (appengine.Value, error) {
	if mv, ok := m.(*appengine.BronzeHistoryGCPAppEngineServiceMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *appengine.BronzeHistoryGCPAppEngineServiceMutation", m)
}

// Condition is a hook condition function.
type Condition func(context.Context, appengine.Mutation) bool

// And groups conditions with the AND operator.
func And(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m appengine.Mutation) bool {
		if !first(ctx, m) || !second(ctx, m) {
			return false
		}
		for _, cond := range rest {
			if !cond(ctx, m) {
				return false
			}
		}
		return true
	}
}

// Or groups conditions with the OR operator.
func Or(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m appengine.Mutation) bool {
		if first(ctx, m) || second(ctx, m) {
			return true
		}
		for _, cond := range rest {
			if cond(ctx, m) {
				return true
			}
		}
		return false
	}
}

// Not negates a given condition.
func Not(cond Condition) Condition {
	return func(ctx context.Context, m appengine.Mutation) bool {
		return !cond(ctx, m)
	}
}

// HasOp is a condition testing mutation operation.
func HasOp(op appengine.Op) Condition {
	return func(_ context.Context, m appengine.Mutation) bool {
		return m.Op().Is(op)
	}
}

// HasAddedFields is a condition validating `.AddedField` on fields.
func HasAddedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m appengine.Mutation) bool {
		if _, exists := m.AddedField(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.AddedField(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasClearedFields is a condition validating `.FieldCleared` on fields.
func HasClearedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m appengine.Mutation) bool {
		if exists := m.FieldCleared(field); !exists {
			return false
		}
		for _, field := range fields {
			if exists := m.FieldCleared(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasFields is a condition validating `.Field` on fields.
func HasFields(field string, fields ...string) Condition {
	return func(_ context.Context, m appengine.Mutation) bool {
		if _, exists := m.Field(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.Field(field); !exists {
				return false
			}
		}
		return true
	}
}

// If executes the given hook under condition.
//
//	hook.If(ComputeAverage, And(HasFields(...), HasAddedFields(...)))
func If(hk appengine.Hook, cond Condition) appengine.Hook {
	return func(next appengine.Mutator) appengine.Mutator {
		return appengine.MutateFunc(func(ctx context.Context, m appengine.Mutation) (appengine.Value, error) {
			if cond(ctx, m) {
				return hk(next).Mutate(ctx, m)
			}
			return next.Mutate(ctx, m)
		})
	}
}

// On executes the given hook only for the given operation.
//
//	hook.On(Log, appengine.Delete|appengine.Create)
func On(hk appengine.Hook, op appengine.Op) appengine.Hook {
	return If(hk, HasOp(op))
}

// Unless skips the given hook only for the given operation.
//
//	hook.Unless(Log, appengine.Update|appengine.UpdateOne)
func Unless(hk appengine.Hook, op appengine.Op) appengine.Hook {
	return If(hk, Not(HasOp(op)))
}

// FixedError is a hook returning a fixed error.
func FixedError(err error) appengine.Hook {
	return func(appengine.Mutator) appengine.Mutator {
		return appengine.MutateFunc(func(context.Context, appengine.Mutation) (appengine.Value, error) {
			return nil, err
		})
	}
}

// Reject returns a hook that rejects all operations that match op.
//
//	func (T) Hooks() []appengine.Hook {
//		return []appengine.Hook{
//			Reject(appengine.Delete|appengine.Update),
//		}
//	}
func Reject(op appengine.Op) appengine.Hook {
	hk := FixedError(fmt.Errorf("%s operation is not allowed", op))
	return On(hk, op)
}

// Chain acts as a list of hooks and is effectively immutable.
// Once created, it will always hold the same set of hooks in the same order.
type Chain struct {
	hooks []appengine.Hook
}

// NewChain creates a new chain of hooks.
func NewChain(hooks ...appengine.Hook) Chain {
	return Chain{append([]appengine.Hook(nil), hooks...)}
}

// Hook chains the list of hooks and returns the final hook.
func (c Chain) Hook() appengine.Hook {
	return func(mutator appengine.Mutator) appengine.Mutator {
		for i := len(c.hooks) - 1; i >= 0; i-- {
			mutator = c.hooks[i](mutator)
		}
		return mutator
	}
}

// Append extends a chain, adding the specified hook
// as the last ones in the mutation flow.
func (c Chain) Append(hooks ...appengine.Hook) Chain {
	newHooks := make([]appengine.Hook, 0, len(c.hooks)+len(hooks))
	newHooks = append(newHooks, c.hooks...)
	newHooks = append(newHooks, hooks...)
	return Chain{newHooks}
}

// Extend extends a chain, adding the specified chain
// as the last ones in the mutation flow.
func (c Chain) Extend(chain Chain) Chain {
	return c.Append(chain.hooks...)
}
