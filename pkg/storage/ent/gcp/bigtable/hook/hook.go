// Code generated by ent, DO NOT EDIT.

package hook

import (
	"context"
	"fmt"

	"danny.vn/hotpot/pkg/storage/ent/gcp/bigtable"
)

// The BronzeGCPBigtableClusterFunc type is an adapter to allow the use of ordinary
// function as BronzeGCPBigtableCluster mutator.
type BronzeGCPBigtableClusterFunc func(context.Context, *bigtable.BronzeGCPBigtableClusterMutation) (bigtable.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeGCPBigtableClusterFunc) Mutate(ctx context.Context, m bigtable.Mutation) (bigtable.Value, error) {
	if mv, ok := m.(*bigtable.BronzeGCPBigtableClusterMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *bigtable.BronzeGCPBigtableClusterMutation", m)
}

// The BronzeGCPBigtableInstanceFunc type is an adapter to allow the use of ordinary
// function as BronzeGCPBigtableInstance mutator.
type BronzeGCPBigtableInstanceFunc func(context.Context, *bigtable.BronzeGCPBigtableInstanceMutation) (bigtable.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeGCPBigtableInstanceFunc) Mutate(ctx context.Context, m bigtable.Mutation) (bigtable.Value, error) {
	if mv, ok := m.(*bigtable.BronzeGCPBigtableInstanceMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *bigtable.BronzeGCPBigtableInstanceMutation", m)
}

// The BronzeHistoryGCPBigtableClusterFunc type is an adapter to allow the use of ordinary
// function as BronzeHistoryGCPBigtableCluster mutator.
type BronzeHistoryGCPBigtableClusterFunc func(context.Context, *bigtable.BronzeHistoryGCPBigtableClusterMutation) (bigtable.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeHistoryGCPBigtableClusterFunc) Mutate(ctx context.Context, m bigtable.Mutation) (bigtable.Value, error) {
	if mv, ok := m.(*bigtable.BronzeHistoryGCPBigtableClusterMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *bigtable.BronzeHistoryGCPBigtableClusterMutation", m)
}

// The BronzeHistoryGCPBigtableInstanceFunc type is an adapter to allow the use of ordinary
// function as BronzeHistoryGCPBigtableInstance mutator.
type BronzeHistoryGCPBigtableInstanceFunc func(context.Context, *bigtable.BronzeHistoryGCPBigtableInstanceMutation) (bigtable.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeHistoryGCPBigtableInstanceFunc) Mutate(ctx context.Context, m bigtable.Mutation) (bigtable.Value, error) {
	if mv, ok := m.(*bigtable.BronzeHistoryGCPBigtableInstanceMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *bigtable.BronzeHistoryGCPBigtableInstanceMutation", m)
}

// Condition is a hook condition function.
type Condition func(context.Context, bigtable.Mutation) bool

// And groups conditions with the AND operator.
func And(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m bigtable.Mutation) bool {
		if !first(ctx, m) || !second(ctx, m) {
			return false
		}
		for _, cond := range rest {
			if !cond(ctx, m) {
				return false
			}
		}
		return true
	}
}

// Or groups conditions with the OR operator.
func Or(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m bigtable.Mutation) bool {
		if first(ctx, m) || second(ctx, m) {
			return true
		}
		for _, cond := range rest {
			if cond(ctx, m) {
				return true
			}
		}
		return false
	}
}

// Not negates a given condition.
func Not(cond Condition) Condition {
	return func(ctx context.Context, m bigtable.Mutation) bool {
		return !cond(ctx, m)
	}
}

// HasOp is a condition testing mutation operation.
func HasOp(op bigtable.Op) Condition {
	return func(_ context.Context, m bigtable.Mutation) bool {
		return m.Op().Is(op)
	}
}

// HasAddedFields is a condition validating `.AddedField` on fields.
func HasAddedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m bigtable.Mutation) bool {
		if _, exists := m.AddedField(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.AddedField(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasClearedFields is a condition validating `.FieldCleared` on fields.
func HasClearedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m bigtable.Mutation) bool {
		if exists := m.FieldCleared(field); !exists {
			return false
		}
		for _, field := range fields {
			if exists := m.FieldCleared(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasFields is a condition validating `.Field` on fields.
func HasFields(field string, fields ...string) Condition {
	return func(_ context.Context, m bigtable.Mutation) bool {
		if _, exists := m.Field(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.Field(field); !exists {
				return false
			}
		}
		return true
	}
}

// If executes the given hook under condition.
//
//	hook.If(ComputeAverage, And(HasFields(...), HasAddedFields(...)))
func If(hk bigtable.Hook, cond Condition) bigtable.Hook {
	return func(next bigtable.Mutator) bigtable.Mutator {
		return bigtable.MutateFunc(func(ctx context.Context, m bigtable.Mutation) (bigtable.Value, error) {
			if cond(ctx, m) {
				return hk(next).Mutate(ctx, m)
			}
			return next.Mutate(ctx, m)
		})
	}
}

// On executes the given hook only for the given operation.
//
//	hook.On(Log, bigtable.Delete|bigtable.Create)
func On(hk bigtable.Hook, op bigtable.Op) bigtable.Hook {
	return If(hk, HasOp(op))
}

// Unless skips the given hook only for the given operation.
//
//	hook.Unless(Log, bigtable.Update|bigtable.UpdateOne)
func Unless(hk bigtable.Hook, op bigtable.Op) bigtable.Hook {
	return If(hk, Not(HasOp(op)))
}

// FixedError is a hook returning a fixed error.
func FixedError(err error) bigtable.Hook {
	return func(bigtable.Mutator) bigtable.Mutator {
		return bigtable.MutateFunc(func(context.Context, bigtable.Mutation) (bigtable.Value, error) {
			return nil, err
		})
	}
}

// Reject returns a hook that rejects all operations that match op.
//
//	func (T) Hooks() []bigtable.Hook {
//		return []bigtable.Hook{
//			Reject(bigtable.Delete|bigtable.Update),
//		}
//	}
func Reject(op bigtable.Op) bigtable.Hook {
	hk := FixedError(fmt.Errorf("%s operation is not allowed", op))
	return On(hk, op)
}

// Chain acts as a list of hooks and is effectively immutable.
// Once created, it will always hold the same set of hooks in the same order.
type Chain struct {
	hooks []bigtable.Hook
}

// NewChain creates a new chain of hooks.
func NewChain(hooks ...bigtable.Hook) Chain {
	return Chain{append([]bigtable.Hook(nil), hooks...)}
}

// Hook chains the list of hooks and returns the final hook.
func (c Chain) Hook() bigtable.Hook {
	return func(mutator bigtable.Mutator) bigtable.Mutator {
		for i := len(c.hooks) - 1; i >= 0; i-- {
			mutator = c.hooks[i](mutator)
		}
		return mutator
	}
}

// Append extends a chain, adding the specified hook
// as the last ones in the mutation flow.
func (c Chain) Append(hooks ...bigtable.Hook) Chain {
	newHooks := make([]bigtable.Hook, 0, len(c.hooks)+len(hooks))
	newHooks = append(newHooks, c.hooks...)
	newHooks = append(newHooks, hooks...)
	return Chain{newHooks}
}

// Extend extends a chain, adding the specified chain
// as the last ones in the mutation flow.
func (c Chain) Extend(chain Chain) Chain {
	return c.Append(chain.hooks...)
}
