// Code generated by ent, DO NOT EDIT.

package hook

import (
	"context"
	"fmt"

	"github.com/dannyota/hotpot/pkg/storage/ent/gcp/monitoring"
)

// The BronzeGCPMonitoringAlertPolicyFunc type is an adapter to allow the use of ordinary
// function as BronzeGCPMonitoringAlertPolicy mutator.
type BronzeGCPMonitoringAlertPolicyFunc func(context.Context, *monitoring.BronzeGCPMonitoringAlertPolicyMutation) (monitoring.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeGCPMonitoringAlertPolicyFunc) Mutate(ctx context.Context, m monitoring.Mutation) (monitoring.Value, error) {
	if mv, ok := m.(*monitoring.BronzeGCPMonitoringAlertPolicyMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *monitoring.BronzeGCPMonitoringAlertPolicyMutation", m)
}

// The BronzeGCPMonitoringUptimeCheckConfigFunc type is an adapter to allow the use of ordinary
// function as BronzeGCPMonitoringUptimeCheckConfig mutator.
type BronzeGCPMonitoringUptimeCheckConfigFunc func(context.Context, *monitoring.BronzeGCPMonitoringUptimeCheckConfigMutation) (monitoring.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeGCPMonitoringUptimeCheckConfigFunc) Mutate(ctx context.Context, m monitoring.Mutation) (monitoring.Value, error) {
	if mv, ok := m.(*monitoring.BronzeGCPMonitoringUptimeCheckConfigMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *monitoring.BronzeGCPMonitoringUptimeCheckConfigMutation", m)
}

// The BronzeHistoryGCPMonitoringAlertPolicyFunc type is an adapter to allow the use of ordinary
// function as BronzeHistoryGCPMonitoringAlertPolicy mutator.
type BronzeHistoryGCPMonitoringAlertPolicyFunc func(context.Context, *monitoring.BronzeHistoryGCPMonitoringAlertPolicyMutation) (monitoring.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeHistoryGCPMonitoringAlertPolicyFunc) Mutate(ctx context.Context, m monitoring.Mutation) (monitoring.Value, error) {
	if mv, ok := m.(*monitoring.BronzeHistoryGCPMonitoringAlertPolicyMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *monitoring.BronzeHistoryGCPMonitoringAlertPolicyMutation", m)
}

// The BronzeHistoryGCPMonitoringUptimeCheckConfigFunc type is an adapter to allow the use of ordinary
// function as BronzeHistoryGCPMonitoringUptimeCheckConfig mutator.
type BronzeHistoryGCPMonitoringUptimeCheckConfigFunc func(context.Context, *monitoring.BronzeHistoryGCPMonitoringUptimeCheckConfigMutation) (monitoring.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeHistoryGCPMonitoringUptimeCheckConfigFunc) Mutate(ctx context.Context, m monitoring.Mutation) (monitoring.Value, error) {
	if mv, ok := m.(*monitoring.BronzeHistoryGCPMonitoringUptimeCheckConfigMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *monitoring.BronzeHistoryGCPMonitoringUptimeCheckConfigMutation", m)
}

// Condition is a hook condition function.
type Condition func(context.Context, monitoring.Mutation) bool

// And groups conditions with the AND operator.
func And(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m monitoring.Mutation) bool {
		if !first(ctx, m) || !second(ctx, m) {
			return false
		}
		for _, cond := range rest {
			if !cond(ctx, m) {
				return false
			}
		}
		return true
	}
}

// Or groups conditions with the OR operator.
func Or(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m monitoring.Mutation) bool {
		if first(ctx, m) || second(ctx, m) {
			return true
		}
		for _, cond := range rest {
			if cond(ctx, m) {
				return true
			}
		}
		return false
	}
}

// Not negates a given condition.
func Not(cond Condition) Condition {
	return func(ctx context.Context, m monitoring.Mutation) bool {
		return !cond(ctx, m)
	}
}

// HasOp is a condition testing mutation operation.
func HasOp(op monitoring.Op) Condition {
	return func(_ context.Context, m monitoring.Mutation) bool {
		return m.Op().Is(op)
	}
}

// HasAddedFields is a condition validating `.AddedField` on fields.
func HasAddedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m monitoring.Mutation) bool {
		if _, exists := m.AddedField(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.AddedField(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasClearedFields is a condition validating `.FieldCleared` on fields.
func HasClearedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m monitoring.Mutation) bool {
		if exists := m.FieldCleared(field); !exists {
			return false
		}
		for _, field := range fields {
			if exists := m.FieldCleared(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasFields is a condition validating `.Field` on fields.
func HasFields(field string, fields ...string) Condition {
	return func(_ context.Context, m monitoring.Mutation) bool {
		if _, exists := m.Field(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.Field(field); !exists {
				return false
			}
		}
		return true
	}
}

// If executes the given hook under condition.
//
//	hook.If(ComputeAverage, And(HasFields(...), HasAddedFields(...)))
func If(hk monitoring.Hook, cond Condition) monitoring.Hook {
	return func(next monitoring.Mutator) monitoring.Mutator {
		return monitoring.MutateFunc(func(ctx context.Context, m monitoring.Mutation) (monitoring.Value, error) {
			if cond(ctx, m) {
				return hk(next).Mutate(ctx, m)
			}
			return next.Mutate(ctx, m)
		})
	}
}

// On executes the given hook only for the given operation.
//
//	hook.On(Log, monitoring.Delete|monitoring.Create)
func On(hk monitoring.Hook, op monitoring.Op) monitoring.Hook {
	return If(hk, HasOp(op))
}

// Unless skips the given hook only for the given operation.
//
//	hook.Unless(Log, monitoring.Update|monitoring.UpdateOne)
func Unless(hk monitoring.Hook, op monitoring.Op) monitoring.Hook {
	return If(hk, Not(HasOp(op)))
}

// FixedError is a hook returning a fixed error.
func FixedError(err error) monitoring.Hook {
	return func(monitoring.Mutator) monitoring.Mutator {
		return monitoring.MutateFunc(func(context.Context, monitoring.Mutation) (monitoring.Value, error) {
			return nil, err
		})
	}
}

// Reject returns a hook that rejects all operations that match op.
//
//	func (T) Hooks() []monitoring.Hook {
//		return []monitoring.Hook{
//			Reject(monitoring.Delete|monitoring.Update),
//		}
//	}
func Reject(op monitoring.Op) monitoring.Hook {
	hk := FixedError(fmt.Errorf("%s operation is not allowed", op))
	return On(hk, op)
}

// Chain acts as a list of hooks and is effectively immutable.
// Once created, it will always hold the same set of hooks in the same order.
type Chain struct {
	hooks []monitoring.Hook
}

// NewChain creates a new chain of hooks.
func NewChain(hooks ...monitoring.Hook) Chain {
	return Chain{append([]monitoring.Hook(nil), hooks...)}
}

// Hook chains the list of hooks and returns the final hook.
func (c Chain) Hook() monitoring.Hook {
	return func(mutator monitoring.Mutator) monitoring.Mutator {
		for i := len(c.hooks) - 1; i >= 0; i-- {
			mutator = c.hooks[i](mutator)
		}
		return mutator
	}
}

// Append extends a chain, adding the specified hook
// as the last ones in the mutation flow.
func (c Chain) Append(hooks ...monitoring.Hook) Chain {
	newHooks := make([]monitoring.Hook, 0, len(c.hooks)+len(hooks))
	newHooks = append(newHooks, c.hooks...)
	newHooks = append(newHooks, hooks...)
	return Chain{newHooks}
}

// Extend extends a chain, adding the specified chain
// as the last ones in the mutation flow.
func (c Chain) Extend(chain Chain) Chain {
	return c.Append(chain.hooks...)
}
