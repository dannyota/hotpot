// Code generated by ent, DO NOT EDIT.

package hook

import (
	"context"
	"fmt"

	"github.com/dannyota/hotpot/pkg/storage/ent/gcp/iap"
)

// The BronzeGCPIAPIAMPolicyFunc type is an adapter to allow the use of ordinary
// function as BronzeGCPIAPIAMPolicy mutator.
type BronzeGCPIAPIAMPolicyFunc func(context.Context, *iap.BronzeGCPIAPIAMPolicyMutation) (iap.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeGCPIAPIAMPolicyFunc) Mutate(ctx context.Context, m iap.Mutation) (iap.Value, error) {
	if mv, ok := m.(*iap.BronzeGCPIAPIAMPolicyMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *iap.BronzeGCPIAPIAMPolicyMutation", m)
}

// The BronzeGCPIAPSettingsFunc type is an adapter to allow the use of ordinary
// function as BronzeGCPIAPSettings mutator.
type BronzeGCPIAPSettingsFunc func(context.Context, *iap.BronzeGCPIAPSettingsMutation) (iap.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeGCPIAPSettingsFunc) Mutate(ctx context.Context, m iap.Mutation) (iap.Value, error) {
	if mv, ok := m.(*iap.BronzeGCPIAPSettingsMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *iap.BronzeGCPIAPSettingsMutation", m)
}

// The BronzeHistoryGCPIAPIAMPolicyFunc type is an adapter to allow the use of ordinary
// function as BronzeHistoryGCPIAPIAMPolicy mutator.
type BronzeHistoryGCPIAPIAMPolicyFunc func(context.Context, *iap.BronzeHistoryGCPIAPIAMPolicyMutation) (iap.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeHistoryGCPIAPIAMPolicyFunc) Mutate(ctx context.Context, m iap.Mutation) (iap.Value, error) {
	if mv, ok := m.(*iap.BronzeHistoryGCPIAPIAMPolicyMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *iap.BronzeHistoryGCPIAPIAMPolicyMutation", m)
}

// The BronzeHistoryGCPIAPSettingsFunc type is an adapter to allow the use of ordinary
// function as BronzeHistoryGCPIAPSettings mutator.
type BronzeHistoryGCPIAPSettingsFunc func(context.Context, *iap.BronzeHistoryGCPIAPSettingsMutation) (iap.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeHistoryGCPIAPSettingsFunc) Mutate(ctx context.Context, m iap.Mutation) (iap.Value, error) {
	if mv, ok := m.(*iap.BronzeHistoryGCPIAPSettingsMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *iap.BronzeHistoryGCPIAPSettingsMutation", m)
}

// Condition is a hook condition function.
type Condition func(context.Context, iap.Mutation) bool

// And groups conditions with the AND operator.
func And(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m iap.Mutation) bool {
		if !first(ctx, m) || !second(ctx, m) {
			return false
		}
		for _, cond := range rest {
			if !cond(ctx, m) {
				return false
			}
		}
		return true
	}
}

// Or groups conditions with the OR operator.
func Or(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m iap.Mutation) bool {
		if first(ctx, m) || second(ctx, m) {
			return true
		}
		for _, cond := range rest {
			if cond(ctx, m) {
				return true
			}
		}
		return false
	}
}

// Not negates a given condition.
func Not(cond Condition) Condition {
	return func(ctx context.Context, m iap.Mutation) bool {
		return !cond(ctx, m)
	}
}

// HasOp is a condition testing mutation operation.
func HasOp(op iap.Op) Condition {
	return func(_ context.Context, m iap.Mutation) bool {
		return m.Op().Is(op)
	}
}

// HasAddedFields is a condition validating `.AddedField` on fields.
func HasAddedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m iap.Mutation) bool {
		if _, exists := m.AddedField(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.AddedField(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasClearedFields is a condition validating `.FieldCleared` on fields.
func HasClearedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m iap.Mutation) bool {
		if exists := m.FieldCleared(field); !exists {
			return false
		}
		for _, field := range fields {
			if exists := m.FieldCleared(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasFields is a condition validating `.Field` on fields.
func HasFields(field string, fields ...string) Condition {
	return func(_ context.Context, m iap.Mutation) bool {
		if _, exists := m.Field(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.Field(field); !exists {
				return false
			}
		}
		return true
	}
}

// If executes the given hook under condition.
//
//	hook.If(ComputeAverage, And(HasFields(...), HasAddedFields(...)))
func If(hk iap.Hook, cond Condition) iap.Hook {
	return func(next iap.Mutator) iap.Mutator {
		return iap.MutateFunc(func(ctx context.Context, m iap.Mutation) (iap.Value, error) {
			if cond(ctx, m) {
				return hk(next).Mutate(ctx, m)
			}
			return next.Mutate(ctx, m)
		})
	}
}

// On executes the given hook only for the given operation.
//
//	hook.On(Log, iap.Delete|iap.Create)
func On(hk iap.Hook, op iap.Op) iap.Hook {
	return If(hk, HasOp(op))
}

// Unless skips the given hook only for the given operation.
//
//	hook.Unless(Log, iap.Update|iap.UpdateOne)
func Unless(hk iap.Hook, op iap.Op) iap.Hook {
	return If(hk, Not(HasOp(op)))
}

// FixedError is a hook returning a fixed error.
func FixedError(err error) iap.Hook {
	return func(iap.Mutator) iap.Mutator {
		return iap.MutateFunc(func(context.Context, iap.Mutation) (iap.Value, error) {
			return nil, err
		})
	}
}

// Reject returns a hook that rejects all operations that match op.
//
//	func (T) Hooks() []iap.Hook {
//		return []iap.Hook{
//			Reject(iap.Delete|iap.Update),
//		}
//	}
func Reject(op iap.Op) iap.Hook {
	hk := FixedError(fmt.Errorf("%s operation is not allowed", op))
	return On(hk, op)
}

// Chain acts as a list of hooks and is effectively immutable.
// Once created, it will always hold the same set of hooks in the same order.
type Chain struct {
	hooks []iap.Hook
}

// NewChain creates a new chain of hooks.
func NewChain(hooks ...iap.Hook) Chain {
	return Chain{append([]iap.Hook(nil), hooks...)}
}

// Hook chains the list of hooks and returns the final hook.
func (c Chain) Hook() iap.Hook {
	return func(mutator iap.Mutator) iap.Mutator {
		for i := len(c.hooks) - 1; i >= 0; i-- {
			mutator = c.hooks[i](mutator)
		}
		return mutator
	}
}

// Append extends a chain, adding the specified hook
// as the last ones in the mutation flow.
func (c Chain) Append(hooks ...iap.Hook) Chain {
	newHooks := make([]iap.Hook, 0, len(c.hooks)+len(hooks))
	newHooks = append(newHooks, c.hooks...)
	newHooks = append(newHooks, hooks...)
	return Chain{newHooks}
}

// Extend extends a chain, adding the specified chain
// as the last ones in the mutation flow.
func (c Chain) Extend(chain Chain) Chain {
	return c.Append(chain.hooks...)
}
