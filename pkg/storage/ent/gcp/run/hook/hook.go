// Code generated by ent, DO NOT EDIT.

package hook

import (
	"context"
	"fmt"

	"github.com/dannyota/hotpot/pkg/storage/ent/gcp/run"
)

// The BronzeGCPRunRevisionFunc type is an adapter to allow the use of ordinary
// function as BronzeGCPRunRevision mutator.
type BronzeGCPRunRevisionFunc func(context.Context, *run.BronzeGCPRunRevisionMutation) (run.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeGCPRunRevisionFunc) Mutate(ctx context.Context, m run.Mutation) (run.Value, error) {
	if mv, ok := m.(*run.BronzeGCPRunRevisionMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *run.BronzeGCPRunRevisionMutation", m)
}

// The BronzeGCPRunServiceFunc type is an adapter to allow the use of ordinary
// function as BronzeGCPRunService mutator.
type BronzeGCPRunServiceFunc func(context.Context, *run.BronzeGCPRunServiceMutation) (run.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeGCPRunServiceFunc) Mutate(ctx context.Context, m run.Mutation) (run.Value, error) {
	if mv, ok := m.(*run.BronzeGCPRunServiceMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *run.BronzeGCPRunServiceMutation", m)
}

// The BronzeHistoryGCPRunRevisionFunc type is an adapter to allow the use of ordinary
// function as BronzeHistoryGCPRunRevision mutator.
type BronzeHistoryGCPRunRevisionFunc func(context.Context, *run.BronzeHistoryGCPRunRevisionMutation) (run.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeHistoryGCPRunRevisionFunc) Mutate(ctx context.Context, m run.Mutation) (run.Value, error) {
	if mv, ok := m.(*run.BronzeHistoryGCPRunRevisionMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *run.BronzeHistoryGCPRunRevisionMutation", m)
}

// The BronzeHistoryGCPRunServiceFunc type is an adapter to allow the use of ordinary
// function as BronzeHistoryGCPRunService mutator.
type BronzeHistoryGCPRunServiceFunc func(context.Context, *run.BronzeHistoryGCPRunServiceMutation) (run.Value, error)

// Mutate calls f(ctx, m).
func (f BronzeHistoryGCPRunServiceFunc) Mutate(ctx context.Context, m run.Mutation) (run.Value, error) {
	if mv, ok := m.(*run.BronzeHistoryGCPRunServiceMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *run.BronzeHistoryGCPRunServiceMutation", m)
}

// Condition is a hook condition function.
type Condition func(context.Context, run.Mutation) bool

// And groups conditions with the AND operator.
func And(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m run.Mutation) bool {
		if !first(ctx, m) || !second(ctx, m) {
			return false
		}
		for _, cond := range rest {
			if !cond(ctx, m) {
				return false
			}
		}
		return true
	}
}

// Or groups conditions with the OR operator.
func Or(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m run.Mutation) bool {
		if first(ctx, m) || second(ctx, m) {
			return true
		}
		for _, cond := range rest {
			if cond(ctx, m) {
				return true
			}
		}
		return false
	}
}

// Not negates a given condition.
func Not(cond Condition) Condition {
	return func(ctx context.Context, m run.Mutation) bool {
		return !cond(ctx, m)
	}
}

// HasOp is a condition testing mutation operation.
func HasOp(op run.Op) Condition {
	return func(_ context.Context, m run.Mutation) bool {
		return m.Op().Is(op)
	}
}

// HasAddedFields is a condition validating `.AddedField` on fields.
func HasAddedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m run.Mutation) bool {
		if _, exists := m.AddedField(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.AddedField(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasClearedFields is a condition validating `.FieldCleared` on fields.
func HasClearedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m run.Mutation) bool {
		if exists := m.FieldCleared(field); !exists {
			return false
		}
		for _, field := range fields {
			if exists := m.FieldCleared(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasFields is a condition validating `.Field` on fields.
func HasFields(field string, fields ...string) Condition {
	return func(_ context.Context, m run.Mutation) bool {
		if _, exists := m.Field(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.Field(field); !exists {
				return false
			}
		}
		return true
	}
}

// If executes the given hook under condition.
//
//	hook.If(ComputeAverage, And(HasFields(...), HasAddedFields(...)))
func If(hk run.Hook, cond Condition) run.Hook {
	return func(next run.Mutator) run.Mutator {
		return run.MutateFunc(func(ctx context.Context, m run.Mutation) (run.Value, error) {
			if cond(ctx, m) {
				return hk(next).Mutate(ctx, m)
			}
			return next.Mutate(ctx, m)
		})
	}
}

// On executes the given hook only for the given operation.
//
//	hook.On(Log, run.Delete|run.Create)
func On(hk run.Hook, op run.Op) run.Hook {
	return If(hk, HasOp(op))
}

// Unless skips the given hook only for the given operation.
//
//	hook.Unless(Log, run.Update|run.UpdateOne)
func Unless(hk run.Hook, op run.Op) run.Hook {
	return If(hk, Not(HasOp(op)))
}

// FixedError is a hook returning a fixed error.
func FixedError(err error) run.Hook {
	return func(run.Mutator) run.Mutator {
		return run.MutateFunc(func(context.Context, run.Mutation) (run.Value, error) {
			return nil, err
		})
	}
}

// Reject returns a hook that rejects all operations that match op.
//
//	func (T) Hooks() []run.Hook {
//		return []run.Hook{
//			Reject(run.Delete|run.Update),
//		}
//	}
func Reject(op run.Op) run.Hook {
	hk := FixedError(fmt.Errorf("%s operation is not allowed", op))
	return On(hk, op)
}

// Chain acts as a list of hooks and is effectively immutable.
// Once created, it will always hold the same set of hooks in the same order.
type Chain struct {
	hooks []run.Hook
}

// NewChain creates a new chain of hooks.
func NewChain(hooks ...run.Hook) Chain {
	return Chain{append([]run.Hook(nil), hooks...)}
}

// Hook chains the list of hooks and returns the final hook.
func (c Chain) Hook() run.Hook {
	return func(mutator run.Mutator) run.Mutator {
		for i := len(c.hooks) - 1; i >= 0; i-- {
			mutator = c.hooks[i](mutator)
		}
		return mutator
	}
}

// Append extends a chain, adding the specified hook
// as the last ones in the mutation flow.
func (c Chain) Append(hooks ...run.Hook) Chain {
	newHooks := make([]run.Hook, 0, len(c.hooks)+len(hooks))
	newHooks = append(newHooks, c.hooks...)
	newHooks = append(newHooks, hooks...)
	return Chain{newHooks}
}

// Extend extends a chain, adding the specified chain
// as the last ones in the mutation flow.
func (c Chain) Extend(chain Chain) Chain {
	return c.Append(chain.hooks...)
}
