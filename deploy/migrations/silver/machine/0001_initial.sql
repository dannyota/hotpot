-- Create "machine_normalized" table
CREATE TABLE "silver"."machine_normalized" (
  "resource_id" character varying NOT NULL,
  "provider" character varying NOT NULL,
  "is_base" boolean NOT NULL,
  "bronze_table" character varying NOT NULL,
  "bronze_resource_id" character varying NOT NULL,
  "hostname" character varying NULL,
  "os_type" character varying NULL,
  "os_name" character varying NULL,
  "status" character varying NOT NULL,
  "internal_ip" character varying NULL,
  "external_ip" character varying NULL,
  "environment" character varying NULL,
  "cloud_project" character varying NULL,
  "cloud_zone" character varying NULL,
  "cloud_machine_type" character varying NULL,
  "merge_keys_json" jsonb NOT NULL,
  "collected_at" timestamptz NOT NULL,
  "first_collected_at" timestamptz NOT NULL,
  "normalized_at" timestamptz NOT NULL,
  PRIMARY KEY ("resource_id")
);
-- Create index "silvermachinenormalized_provider" to table: "machine_normalized"
CREATE INDEX "silvermachinenormalized_provider" ON "silver"."machine_normalized" ("provider");
-- Create index "silvermachinenormalized_provider_bronze_resource_id" to table: "machine_normalized"
CREATE UNIQUE INDEX "silvermachinenormalized_provider_bronze_resource_id" ON "silver"."machine_normalized" ("provider", "bronze_resource_id");
-- Create "machines" table
CREATE TABLE "silver"."machines" (
  "resource_id" character varying NOT NULL,
  "collected_at" timestamptz NOT NULL,
  "first_collected_at" timestamptz NOT NULL,
  "normalized_at" timestamptz NOT NULL,
  "hostname" character varying NOT NULL,
  "os_type" character varying NOT NULL,
  "os_name" character varying NULL,
  "status" character varying NOT NULL,
  "internal_ip" character varying NULL,
  "external_ip" character varying NULL,
  "environment" character varying NULL,
  "cloud_project" character varying NULL,
  "cloud_zone" character varying NULL,
  "cloud_machine_type" character varying NULL,
  PRIMARY KEY ("resource_id")
);
-- Create index "silvermachine_collected_at" to table: "machines"
CREATE INDEX "silvermachine_collected_at" ON "silver"."machines" ("collected_at");
-- Create index "silvermachine_environment" to table: "machines"
CREATE INDEX "silvermachine_environment" ON "silver"."machines" ("environment");
-- Create index "silvermachine_os_type" to table: "machines"
CREATE INDEX "silvermachine_os_type" ON "silver"."machines" ("os_type");
-- Create "machine_bronze_links" table
CREATE TABLE "silver"."machine_bronze_links" (
  "id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY,
  "provider" character varying NOT NULL,
  "bronze_table" character varying NOT NULL,
  "bronze_resource_id" character varying NOT NULL,
  "silver_machine_bronze_links" character varying NOT NULL,
  PRIMARY KEY ("id"),
  CONSTRAINT "machine_bronze_links_machines_bronze_links" FOREIGN KEY ("silver_machine_bronze_links") REFERENCES "silver"."machines" ("resource_id") ON UPDATE NO ACTION ON DELETE NO ACTION
);
